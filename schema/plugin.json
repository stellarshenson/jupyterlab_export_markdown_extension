{
  "title": "Export Markdown Extension",
  "description": "Settings for Markdown export to PDF, DOCX, and HTML",
  "jupyter.lab.setting-icon": "ui-components:markdown",
  "jupyter.lab.setting-icon-label": "Export Markdown",
  "type": "object",
  "properties": {
    "diagramDPI": {
      "title": "Diagram DPI",
      "description": "Resolution for Mermaid diagram images captured from the browser (dots per inch). Uses calibrated scaling matching jupyterlab_mmd_to_png_extension. Standard values: 150 (standard), 300 (high quality print).",
      "type": "number",
      "default": 150,
      "minimum": 72,
      "maximum": 600
    },
    "svgDPI": {
      "title": "SVG Conversion DPI",
      "description": "Resolution for server-side SVG to PNG conversion in DOCX and PDF exports (dots per inch). Applies to all SVG images embedded in markdown. Standard values: 150 (standard), 300 (high quality print).",
      "type": "number",
      "default": 150,
      "minimum": 72,
      "maximum": 600
    },
    "mathDPI": {
      "title": "Math DPI",
      "description": "Resolution for LaTeX math expression images in PDF exports (dots per inch). DOCX uses native OMML equations (no images). HTML uses KaTeX client-side rendering. Standard values: 150 (standard), 200 (default), 300 (high quality print).",
      "type": "number",
      "default": 200,
      "minimum": 96,
      "maximum": 600
    },
    "showAlertLabels": {
      "title": "Show Alert Labels",
      "description": "Show alert type labels (NOTE, TIP, IMPORTANT, WARNING, CAUTION) in exported documents. When disabled, only the alert content is shown with colored styling.",
      "type": "boolean",
      "default": false
    }
  },
  "additionalProperties": false
}
