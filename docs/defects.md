# Defects - jupyterlab_export_markdown_extension

`[ ]` open, `[x]` fixed. Dated notes under each track how it evolved.

## Authors

- `@kj` Konrad Jelen

## Markdown element rendering `MARK`

- [x] `DEF-MARK-1` **task-list checkboxes not rendered** - MAJOR; `- [x]` / `- [ ]` items exported as literal `[x]` / `[ ]` text in DOCX and PDF (and HTML); cause: the `markdown.Markdown` call uses no task-list extension so the marker passes through verbatim; fix: preprocess task markers to Word's native checkbox glyphs (done U+2612, open U+2610), tag the glyph run MS Gothic in DOCX (Word's own checkbox font) so it renders solid, DejaVu covers them in PDF, browsers in HTML; `routes.py`
  - log: 2026-07-22T00:00:00Z @kj reported: "checkboxes are not rendered properly neither in docx nor in pdf"; reproduced - `markdown_to_html('- [x] done')` yields `<li>[x] done</li>`
  - log: 2026-07-22T00:00:00Z @kj attempted: rendered as ballot-check U+2611 / U+2610 - user rejected ("use different way"); the U+2611 fell back from Calibri and read thin
  - log: 2026-07-22T00:00:00Z @kj fixed: settled on U+2612 / U+2610 (Word's native checkbox default, also the pandoc/LaTeX convention), glyph run tagged MS Gothic in `style_docx_task_checkboxes`, `preprocess_task_lists` emits the glyphs; mutation-proved across HTML/DOCX/PDF
  - log: 2026-07-23T00:00:00Z @kj hardened: confirming adversarial round found a bare `- [ ]` / `- [x]` with no trailing label passed through literal - the marker regex required whitespace after the bracket; relaxed the tail to `\](?:\s+|$)`; regression test `test_empty_task_item_converts` (known remaining limitation: a task marker inside a 4-space-indented, non-fenced code block is still rewritten)
  - log: 2026-07-23T00:00:00Z @kj hardened: a later round found the fence tracker toggled on the fence char alone, so a shorter ```inside a longer ```` block closed it early and rewrote task markers that were still code; now tracks fence length and closes only on a same-char fence at least as long as the opener (CommonMark); regression test`test_task_marker_in_nested_longer_fence_not_converted`, mutation-proved
  - log: 2026-07-23T00:00:00Z @kj hardened: a fourth round found a same-length ` ```lang ` line inside a block still closed it (a closing fence carries no info string per CommonMark), rewriting still-code markers; the close now also requires a bare fence (nothing after the backticks); regression test `test_task_marker_after_info_string_fence_not_converted`, mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-2` **blockquotes lose indent, bar and shading in PDF** - MEDIUM; `>` quotes rendered correctly in DOCX but in PDF appeared as plain gray italic text with no left indent, no left bar and no background; cause: `convert_docx_to_pdf.process_paragraph` read run formatting only and ignored the `w:pBdr` / `w:shd` / `w:ind` that `style_docx_blockquotes` applied; fix: detect the blockquote paragraph (left `w:pBdr`) and render it as a left-barred, shaded, indented callout in reportlab; `routes.py`
  - log: 2026-07-22T00:00:00Z @kj reported: "'>' quotes are not rendered properly in the pdf, but are rendered properly in the docx"; reproduced - blockquote text sits at x0=42 (same as body), no bar/shading
  - log: 2026-07-22T00:00:00Z @kj fixed: `make_callout` + `blockquote_info` in `convert_docx_to_pdf`; quote text now indents with a gray bar (BBBBBB) and F4F4F4 shading; mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-3` **alert boxes render as a table header in PDF** - MEDIUM; GitHub alerts rendered correctly in DOCX but in PDF showed as a bold blue content-table header cell with grid lines, losing the colored left border and shading; cause: the alert single-cell table reached `convert_docx_to_pdf.process_table`, which styles every table's first row as a header; fix: detect the alert table (1x1, single colored left border, cell shading) and render it as a colored-left-bar shaded callout with normal text; `routes.py`
  - log: 2026-07-22T00:00:00Z @kj reported: "github style alert boxes (especially multiline) are not rendered properly in pdf, are rendered properly in docx"; reproduced - alert content rendered bold #365f91 (header color), purple border 8250DF lost
  - log: 2026-07-22T00:00:00Z @kj fixed: `alert_info` + `process_alert` in `convert_docx_to_pdf`; alert now renders as a purple-bar F4EDFF-shaded callout with normal black text; a real data table keeps its header styling; mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [x] `DEF-MARK-10` **an explicit line break renders as two, breaking Q&A grouping** - MEDIUM; a line ended with `<br>` (the idiom for a question above its answer) came out with a blank line after it, so the gap INSIDE a Q&A pair exceeded the gap between pairs and the grouping read backwards; cause: `markdown_to_html` enables the `nl2br` extension, which turns the newline after the author's `<br>` into a second break; fix: a replacement for the `nl2br` rule that skips the newline directly after a break the author typed, registered at `nl2br`'s own inline slot in `markdown_to_html` so all three formats inherit it; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj reported: "question and answer in the FAQ section are together and separated from next q & a; in the docx they are spread apart, one cannot know that one q&a is separate from next q&a"; example `01-scoping-poc-system-components.md`
  - log: 2026-07-23T00:00:00Z @kj reproduced: the HTML carried `<br><br />` per pair, the DOCX two `w:br` in one paragraph. Measured in the PDF: **24.0pt between a question and its answer against 18.0pt to the next question** - the pair was the widest gap on the page. In Word the same inversion, ~25pt inside the pair against the ~23pt the document's `w:after="200"` plus 1.15 line spacing puts between paragraphs
  - log: 2026-07-23T00:00:00Z @kj fixed (v1.6.19): shipped as a regex over the finished HTML, collapsing a `<br>` followed by the generated `<br />`. PDF measured 12.0pt inside the pair against 18.0pt between pairs, DOCX one `w:br` per pair; verified on the reference document across all 16 Q&A pairs
  - log: 2026-07-23T00:00:00Z @kj replaced twice under adversarial review: the HTML-shape regex could not tell an authored break from a generated one, so it deleted real breaks inside a raw HTML block and for Markdown's two-trailing-spaces hard break, and missed `<BR>`, attributes and a trailing space. The rule moved to the inline stage where provenance still exists - see `manual_break_aware_nl2br`
  - log: 2026-07-23T00:00:00Z @kj held: fifteen regression tests - HTML, DOCX `w:br` count, PDF gap geometry, explicit blank line, table cell, raw HTML block, trailing space, non-breaking space, `<BR>` and attribute spellings, custom element `<br-spacer>`, two-space hard break, a `<br>` alone on its line, a comment merely mentioning a break, and a deliberately broken rule still returning 200 - each mutation-proved. Known limitation, pinned by test: a break inside inline markup (`**Q<br>**`) is not detected. 151 tests pass; the reference document still measures a uniform 12.0 / 18.0 across all 16 pairs
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [x] `DEF-MARK-11` **a multi-paragraph GitHub alert splits into two boxes** - MAJOR; `> [!NOTE]` whose body has two paragraphs (separated by a bare `>` line) renders as a coloured alert box holding only the first paragraph, with the second following as a separate grey-barred blockquote box; cause: `alert_pattern`'s continuation group is `(?:> .*\n?)*`, which requires `"> "` with a space, so the bare `>` that separates paragraphs terminates the match; fix: accept a bare `>` as a continuation line, and carry the body's own structure in explicit breaks - one break per source newline (what `nl2br` already gives body text), two for the bare `>` that separates paragraphs; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj found by the UX lens during the DEF-MARK-10 review; reproduced - `> [!NOTE]\n> First.\n>\n> Second.` exports one alert table containing "First." and leaves "Second." as a loose paragraph. Affects all three formats
  - log: 2026-07-23T00:00:00Z @kj fixed: the marker has to sit in one paragraph for the DOCX and HTML passes to find it, so the body is emitted as one paragraph with explicit breaks rather than as several. The PDF needed no change - `process_alert` already walks every paragraph in the alert cell. Seven regression tests (HTML box count and content, DOCX alert-table cell text, PDF callout with no stray grey blockquote bar, line breaks kept, an authored `<br>` not doubled, two adjacent alerts staying separate, a plain blockquote not captured); mutation-proved on both the regex and the join
  - log: 2026-07-23T00:00:00Z @kj visible behaviour change: an alert whose prose is soft-wrapped across source lines now breaks at those newlines, as body text already did. Alerts were the only place a source newline was silently discarded
  - log: 2026-07-23T00:00:00Z @kj known limitation: block structure inside an alert (a list, a nested fence, a heading) is still flattened into the one paragraph - it was before this change too. Registered as `DEF-MARK-14`
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-12` **PDF renders Heading 4, 5 and 6 identically to Heading 3** - MEDIUM; `convert_docx_to_pdf` routes every style whose name starts with `Heading` into `heading3_style`, so a sub-subsection reads as a sibling of its parent; the DOCX keeps all four distinct (H3 bold, H4 bold italic, H5 regular, H6 italic, with two colours); fix: a `ParagraphStyle` per level, faces read off the DOCX template python-docx builds from, dispatched on the level number parsed out of the style name; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj found by the UX lens during the DEF-MARK-10 review; measured - `### / #### / #####` all render 11.0pt DejaVuSans-Bold #4f81bd in the PDF. The two formats disagree on the same document
  - log: 2026-07-23T00:00:00Z @kj fixed: `PDF_MINOR_HEADING_FACES` gives H4 bold italic #4F81BD, H5 regular #243F60, H6 italic #243F60, each at body size - the DOCX template's own faces, where the levels are told apart by weight, slant and colour rather than by size. An unnumbered or out-of-range `Heading ...` style still falls to the Heading 3 face, as before. Three regression tests (all six levels visually distinct, the three minor faces matching the DOCX, minor headings at body size with the size descending into H4); mutation-proved
  - log: 2026-07-23T00:00:00Z @kj related, same lens, still open: no `keepWithNext` on any PDF heading style, so a heading can be stranded at the foot of a page (Word's heading styles all carry `keep_with_next`). Registered as `DEF-MARK-15`
  - log: 2026-07-23T00:00:00Z @kj hardened under adversarial review: the UX lens measured the new italic heading faces (H4/H6) rendering in a Helvetica core font on this box, a typeface switch mid-ladder - `_register_unicode_fonts` committed to the first family with a normal file (DejaVu, best coverage) and stopped, and DejaVu ships no oblique here, so every italic fell back to `Helvetica-Oblique`. Now each of the four variants is filled from the first family that ships it: normal and bold stay on DejaVu (the arrow/symbol coverage the body needs), the italics come from Liberation. New test `test_italic_headings_do_not_fall_back_to_a_core_font`, mutation-proved; `test_minor_headings_match_the_docx_faces` was rewritten to read the exported DOCX's own heading colours rather than a copy, so a python-docx template bump trips CI instead of silently splitting the two formats
  - log: 2026-07-23T00:00:00Z @kj accepted (adversarial review, not fixed): H5 is regular navy `#243F60` at body size, distinguished from black body text by colour alone (~2:1 luminance) - copied faithfully from Word's own Heading 5, so changing it would break the very PDF/DOCX parity DEF-MARK-12 exists for; and the heading colours remain hardcoded in the PDF constants as well as the DOCX template and the HTML stylesheet (three copies), matching the file's pre-existing H1-H3 convention - the rewritten test now enforces PDF-equals-DOCX so drift is caught rather than derived away. Palette triplication registered as `DEF-MARK-20`
  - log: 2026-07-23T00:00:00Z @kj accepted (confirming UX round): a decorative dingbat (U+2605, U+2713) written inside an italic span now draws as a notdef box - Liberation (the italic family) covers arrows and bullets but not those glyphs, where the old path substituted a Symbol font mid-run. Not a regression for the symbols the extension itself generates (arrows in italic are covered, and all symbols in normal/bold text stay on DejaVu), and rare (a decorative glyph literally inside `*emphasis*`). Left as-is
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [x] `DEF-MARK-13` **a long code line runs past the right margin in PDF** - MAJOR; a fenced code block wider than the frame is drawn past the page margin instead of wrapping; cause: `highlight_code_for_pdf` builds `Preformatted`/`XPreformatted` without a `maxLineLength`, so the flowable has no width to wrap against; fix: the code font is fixed-width, so the frame width converts to an exact column count and each source line is split to it before the highlighting markup is put on; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj found while reviewing the export font size feature; measured on a 135-character line - rightmost ink at x=614.4 against a 576pt margin with 8pt code (the size the PDF used before the feature, and what `small` gives today), x=601.0 at the 12pt default, x=585.6 at `large`. Pre-existing and slightly less severe at the new default, so not a regression of that change
  - log: 2026-07-23T00:00:00Z @kj measured worse than reported: reportlab does not merely draw the overflow past the margin, it draws it past the page edge, where it is not rendered at all. A 300-character line put **91 of its 300 characters** on the page and silently dropped the other 209. `XPreformatted.breakLines` lays every source line out as exactly one line whatever its width, and no style setting - including the `wordWrap='CJK'` already on the code style - changes that
  - log: 2026-07-23T00:00:00Z @kj fixed: `code_columns` converts the frame width to a column count via `stringWidth` on the code font, and the token loop now carries raw `(colour, text)` segments per source line so the split happens on real characters and the `<font>` markup is applied to the pieces afterwards. The pygments-absent fallback passes the same count to `Preformatted(maxLineLength=)`. Three regression tests (nothing past the frame edge, no characters lost, a line that fits is not broken); mutation-proved
  - log: 2026-07-23T00:00:00Z @kj verified on the reference documents: before the fix `00-inception-poc-owt.md` put 16/22/20 text runs past the margin at small/medium/large, `01-scoping-poc-mockup-flow.md` 2 at large - the mermaid source fences. After, zero across all four documents at all three sizes
  - log: 2026-07-23T00:00:00Z @kj accepted limitation (adversarial review): the column count assumes the code font's fixed width, so a line dense with characters the code font lacks - an emoji, a CJK glyph - renders through a wider substitute and can still overrun; rare in code, and still far better than the whole line running off the page, which was the reported failure for ASCII. A wrapped continuation line carries no hanging indent or marker, the universal soft-wrap trade-off; a reader cannot tell it from a new statement. Both stated in the `code_columns` docstring, neither fixed
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [ ] `DEF-MARK-14` **block structure inside a GitHub alert is flattened** - MAJOR; a list, nested fence or heading written inside a `> [!NOTE]` body renders as run-on text: the alert is emitted as a single markdown paragraph, because the marker that the DOCX and HTML passes key on has to sit in one paragraph; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj registered while fixing `DEF-MARK-11`, which made the paragraph structure visible but not the block structure. Pre-dates that change. A real fix needs an opening and closing marker and a body-element sweep in `style_docx_alert_boxes` and `style_html_alert_boxes`; the PDF's `process_alert` already walks every paragraph in the cell
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-15` **a PDF heading can be stranded at the foot of a page** - MEDIUM; no PDF heading style sets `keepWithNext`, and reportlab's default is off, so a page break can fall between a heading and its first paragraph; every DOCX Heading style carries `keep_with_next`, so Word never does this; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj registered during the `DEF-MARK-12` fix, found earlier by the UX lens during the DEF-MARK-10 review. Not fixed with DEF-MARK-12: a separate defect about pagination, not about which face a level gets
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-20` **heading colour palette hardcoded in three places** - MEDIUM; `#365F91` / `#4F81BD` / `#243F60` live independently in the PDF heading `ParagraphStyle`s, the HTML-export stylesheet, and python-docx's bundled template; a template bump moves the DOCX while the PDF and HTML stay put; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj registered from the adversarial review of `DEF-MARK-12` - tech debt, not a rendering defect (all three agree today). The rewritten `test_minor_headings_match_the_docx_faces` catches drift for the PDF H4-H6 slice only; H1-H3 and the HTML CSS remain unguarded. A real fix derives the faces from `doc.styles['Heading N']` once, at build time
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-22` **a symbol glyph in DOCX is left to Word's font substitution** - MEDIUM; a character Cambria has no glyph for (U+2605, U+2194, `◦`, `▪`, box-drawing, dingbats) carried no font of its own, so each reader's Word substituted from whatever that machine had installed; the same file drew a solid star on one PC and a hollow box on the next; fix: `style_docx_symbol_runs` names a font on exactly those characters - the pass that already gave task checkboxes MS Gothic, generalised to a range table - so the choice is made once, in the file; `routes.py`
  - log: 2026-08-25T00:00:00Z @kj reported against a court filing carrying 31 U+2605 evidence markers and 136 `·` separators; measured on the export: every star run came out with no `w:rFonts` at all
  - log: 2026-08-25T00:00:00Z @kj fixed: `DOCX_SYMBOL_RANGES` lists only the blocks Cambria is empty in, and the arrow block deliberately starts at U+2194 - Cambria does carry U+2190-2193, so an everyday U+2192 keeps the body typeface instead of switching mid-sentence. Verified on the reported document: 31 star runs now name `Segoe UI Symbol`, the 46 middot runs stay in the body face (Cambria draws `·`), no text lost to the run splitting. Mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-23` **inline HTML in the markdown reaches Word as unstyled text** - MEDIUM; htmldocx reads a handful of tags and two CSS properties, so `<span style="font-weight:bold">`, `<mark>`, `<del>`, `<kbd>`, `<font color>` and an `align=` attribute all arrived in the DOCX and PDF as plain text; a `<div>` was worse than dropped - having no handler, its content joined whatever paragraph was already open, so two blocks ran together and one alignment overwrote the other; fix: `restructure_html_for_docx` rewrites each into the markup htmldocx does read, which the PDF inherits because it builds from the same intermediate DOCX; `routes.py`
  - log: 2026-08-25T00:00:00Z @kj reported alongside `DEF-MARK-22` from the same filing, which colours its separators with `<span style="color:#e8e8e8">`. Colour itself already worked; measured the neighbours and found every sibling property silently dropped
  - log: 2026-08-26T00:00:00Z @kj fixed the PDF half: `process_paragraph` walked `para.runs`, which is `./w:r` and so never returns a run Word nested in `<w:hyperlink>` - a highlighted paragraph holding a link lost the link label outright. A table cell was worse: `cell_content` rebuilt it from `cell_markup(p.text)`, a plain-string projection, so bold, italic, colour and the mark fill all reached Word and none reached the PDF. Both now run through `docx_paragraph_runs` + `format_run`. Measured: `CELLMARK` gains a yellow fill at the cell's own x0=48.0, `GREYCELL` its `#e8e8e8`, `ITALCELL` an italic face, and a cell `[CELLLINK](...)` keeps its label at link colour `#0000ee`
  - log: 2026-08-26T00:00:00Z @kj the cell fix opened one of its own and it was caught before ship: rendering a cell's run formatting widened its text without telling the column measurement, which still sized every body cell in the regular face, so reportlab hard-split `**bold**` into `bol` and `d` on two lines. `string_width` now measures in the bold face for every row - widening one column costs its neighbours nothing when all are measured alike and `fit_column_widths` renormalises. Mutation-proved
  - log: 2026-08-25T00:00:00Z @kj fixed: `font-weight` / `font-style` / `text-decoration` become real `<b>` / `<i>` / `<u>` / `<s>` wrappers (around the contents when the element is a block, which cannot sit inside `<b>` and still open its own paragraph), `<mark>` becomes a yellow background pill, `align=` becomes `text-align`, and a `<div>` either becomes the paragraph it stands for or hands its alignment to the blocks it wraps and unwraps. Ten regression tests, mutation-proved in three slices
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-24` **`font-size`, `font-family` and `<small>` are still dropped in DOCX** - MEDIUM; htmldocx has no path for either CSS property, so `<span style="font-size:20pt">` and `<span style="font-family:Courier New">` arrive at body size in the body face, and `<small>` is not distinguished at all; `routes.py`
  - log: 2026-08-25T00:00:00Z @kj registered while fixing `DEF-MARK-23`. Not fixed with it: every property closed there had an equivalent tag to be rewritten into, which is why that fix needed no new machinery. These have none, so they need a marker and a post-conversion run pass of their own - the shape `style_docx_color_runs` already uses for background fills
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-25` **PDF paragraph alignment reaches a body paragraph and a heading only** - MEDIUM; `process_paragraph` now maps `para.alignment` onto the normal and heading styles, so a centred `<div>` draws centred (x0 42.0 -> 265.7 on a 612pt page); the list, blockquote and table-cell branches keep the indent their own style sets and are not passed the alignment, so a centred item inside one still draws at that style's left edge (measured: centred `<li>` x0=60.0, centred blockquote x0=75.6); `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered while fixing the PDF half of `DEF-MARK-23`. The body-paragraph and heading cases were the reported divergence and are closed; the three indented branches each build their own flowable (a bullet prefix, a callout frame, a cell), so carrying alignment into them is a separate change with its own layout surface
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-26` **inline code loses its monospace on any non-ASCII character** - MEDIUM; `strip_monospace_from_unicode_runs` drops a run's `w:rFonts` when `not text.isascii()`, a predicate about the alphabet rather than about which glyphs Courier has; `` `zazolc.py` `` keeps Courier and `` `zażółć.py` `` in the same sentence loses it, `` `100 EUR` `` keeps it and `` `100 €` `` loses it, and an everyday U+2192 costs the whole span because U+2192 sits below the symbol table's U+2194 floor and so is never split off; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the confirming review of `DEF-MARK-22`/`DEF-MARK-23`. Not a regression - measured byte-for-byte identical against `git archive HEAD`, and the function itself is unchanged in the diff; only its call site moved. For a Polish corpus this is the common case, not the edge case
  - log: 2026-08-26T00:00:00Z @kj the proposed merge into `style_docx_symbol_runs` was built and measured, not argued: the arrow case improves, but Polish regresses to a typeface alternating mid-word inside one identifier, and a two-paragraph Polish document goes from 10 runs to 179. The single-predicate elegance is what pays for that, because the third answer cannot be given from the character alone. Rejected. Both narrower alternatives (a codepoint floor near U+024F, or deleting the pass) rest on Windows glyph-coverage facts not measurable here
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-27` **the symbol allow-list is an inventory, not a rule** - MEDIUM; `DOCX_SYMBOL_CHARS` names six characters one at a time because the blocks they live in interleave text symbols with emoji and cannot be taken whole; the six are the ones the reported filing uses, so U+2605 is stable and U+2B51 is not, U+2717 is stable and U+2716 is not, and U+2611 gets no font while U+2610/U+2612 beside it in the same task list get MS Gothic; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the confirming review. Two of three lenses hold that a hand-list beats building a font-introspection layer for six characters; the third measured that the narrowing dropped 1046 text-presentation codepoints across those blocks to dodge 58 real emoji. The mechanical form is a static `Emoji_Presentation` table, which is a dependency and a data file this project does not otherwise carry
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-28` **`<kbd>` and `<samp>` are unstyled in the HTML export** - MEDIUM; the stylesheet has selectors for `code` and `pre code` only, so `<code>` draws as a grey chip at 0.85em and `<kbd>` as bare browser monospace at full size, from the same author intent; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the confirming review. The `DEF-MARK-23` alias that makes `<kbd>` behave like `<code>` lives in `restructure_html_for_docx`, which the HTML handler never calls - so the DOCX and PDF halves are consistent and only HTML is not. One CSS selector, but outside a change scoped to DOCX and PDF
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-42` **a top-level list indented two spaces exports as literal markdown source** - MAJOR; the table of contents in `04-sow-broilers-field-poc-delivery.md` (16 lines matching `^  [-*+] `) rendered correctly in every markdown viewer but exported as a block of `- [1. Introduction](#1.-Introduction)` source in DOCX, PDF and HTML; cause: `markdown_to_html` builds `markdown.Markdown` with `tab_length=2` so a two-space NESTED list nests, and `tab_length` is also python-markdown's indented-code threshold, so a TOP-LEVEL marker two spaces in is classified as an indented code block; fix: `normalize_list_indentation` shifts such a block back to column 0 before the converter sees it; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj measured both sides of the trade, so neither `tab_length` value alone is right: at 2, `para\n\n  - item` yields `<pre><code>- item` and at 4 it yields `<ul><li>item`; at 4, `- top\n  - nested` flattens into two siblings and at 2 it nests. CommonMark allows up to three spaces before a marker without leaving the top level and sets the list's content column from its FIRST marker, so the indentation is a source-level question, not a converter setting
  - log: 2026-08-27T00:00:00Z @kj fixed: the block is dedented by exactly its first marker's indent and every line in it moves by the same amount, so nesting inside it is a pure translation; the block ends at the first non-blank line left of that column, which is what guarantees no line is clipped. Four or more spaces is never a candidate, and neither is a marker inside a list that is already open - the open list is tracked with the `_list_content_col` idiom `iter_mermaid_blocks` uses, or a loose list's nested item (`- top`, blank, `  - nested`) would be flattened into a sibling. Fenced code is tracked by fence character and length, the way `preprocess_task_lists` tracks it, and a fenced body only ever travels with the block that holds it
  - log: 2026-08-27T00:00:00Z @kj placement: called from all three handlers between the mermaid passes and the code-block passes, not from the shared `markdown_to_html`. Both ends of that window are measured - the mermaid scanners pair diagrams with the frontend by fence position (`DEF-DIAG-16`), so nothing may move before them; and by the time DOCX reaches `markdown_to_html` its fences are already Pygments HTML, where a `  - item` inside a `<pre>` survives verbatim and was measurably rewritten by a normalisation running there
  - log: 2026-08-27T00:00:00Z @kj tests: nine regressions in `TestIndentedTopLevelList`, all end-to-end through the export API. Mutation-proved on four separate reverts - the whole pass off (5 fail, including all three formats), the open-list guard off (loose nesting flattens), the fence branch off (a fenced sample is dedented), and the candidate indent widened to four (an indented code block loses its content)
  - log: 2026-08-27T00:00:00Z @kj hardened, from the confirming review: the fence branch ran BEFORE the block-end test and `continue`d, so a fence written left of the open marker column neither closed the block nor escaped it - `  - item`, blank, then a column-0 fence clipped `    return 1` in the sample to `  return 1` in all three formats, and while the fence stayed open there was no code path at all that could clear the shift, so a single unclosed fence flattened every list to end of file. The block-end test now runs first, on the fence line as on any other, which also makes the branch's `if indent >= shift` arm unreachable and lets it go. Four regressions, mutation-proved: reverted, the DOCX carries `'FSteps:do thisdef f():  return 1'` where four spaces were written
  - log: 2026-08-27T00:00:00Z @kj widened, from the same review: the candidate test also required a preceding blank line, so the commonest shape of all - a list written directly under its heading or its lead-in - still exported as literal source. `prev_blank` moved off the candidate test and onto the list_col reset instead, which is where it was actually load-bearing: an item's paragraph may run on at column 0 without ending the item, and reading that lazy continuation as a close made the item's own nested marker below look top-level. Measured over 8,928 markdown files under `/home/lab/workspace` - 15 files gain a list where they had literal source, 102 change only whitespace inside a paragraph, none regress. The reviewer's own remedy (drop `prev_blank`, keep `list_col`) was refuted by that same corpus: it flattened a genuine nested item in four files, `81_blackwell_gemm_blockwise/README.md` among them
  - log: 2026-08-27T00:00:00Z @kj edited severity
  - log: 2026-08-27T17:37:14Z @kj redesigned after round 11: the refusal predicate (60 lines predicting the converter) replaced by a rendered-document check - the shift is kept only when exactly one top-level block changes, from an indented code block made of exactly this chunk into a list holding nothing that needs a block of its own; every guard from rounds 6-11 is a special case of it; pass 329 -> 130 lines
- [x] `DEF-MARK-43` **an HTML callout box renders as plain unstyled text in DOCX and PDF** - MEDIUM; the DRAFT warning at the top of `04-sow-broilers-field-poc-delivery.md` (`<div style="border: 2px dashed #9ca3af; background: rgba(244, 244, 245, 0.45); padding: 10px 14px; ...">`) draws as a dashed grey box in every markdown viewer and in the HTML export, and exported as a single `Normal` paragraph with no box, no border and no background: `restructure_html_for_docx` renames the childless div to `<p>` and carries the style attribute forward, but nothing downstream reads border, background, padding or margin; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj fixed by reusing the callout machinery rather than adding a second one: a `<div>` carrying a border or a background takes a `⁣BOX:<bar>:<fill>⁣` marker in `restructure_html_for_docx`, and `style_docx_alert_boxes` boxes it in exactly the single-cell table a GitHub alert gets, in the colours read off the div's own CSS. The PDF needed no new code - `alert_info` reads those colours back out of the finished DOCX and `make_callout` draws them, so the two formats cannot drift apart
  - log: 2026-08-27T00:00:00Z @kj fidelity chosen: the box, its border colour and its fill; not the dash pattern, the 2px all-round geometry or the alpha. Matching those means a second callout mechanism on the PDF side, which draws one left rule; the reader's question is whether there is a box where the author drew one and what colour it is
  - log: 2026-08-27T00:00:00Z @kj boundaries, each measured rather than assumed: only a div that is not inside a `<li>`, `<td>` or heading, because Word has no table inside a paragraph and boxing one there swallows the whole item; never a div holding a picture, because htmldocx gives a picture a paragraph of its own, which stays outside the table the text is moved into and so lands below the box it was written above; and the background declaration leaves the style attribute with the marker, or the pill pass would also shade every run - a highlighter band across text already sitting on the box's fill. `alert_info` no longer declines a pure-black bar: a hand-drawn border may be written in black, and what keeps an ordinary table out is the 1x1 shape and the blank other sides
  - log: 2026-08-27T00:00:00Z @kj real document: the six hand-drawn callouts (the DRAFT box and five internal notes) export as boxes barred `9CA3AF` on `F4F4F5` in the DOCX and as six PDF boxes with that same bar and fill; the three GitHub alerts keep their own colours beside them; no marker reaches the text of either format
  - log: 2026-08-27T00:00:00Z @kj known limit: a div whose children are explicit block tags (`<div style="..."><p>a</p><p>b</p></div>`) is still unwrapped without a box, the callout construct being one paragraph per marker. A div written the markdown way does not take that path - python-markdown keeps the whole block raw, blank lines included, so it arrives as one childless div and is boxed
  - log: 2026-08-27T00:00:00Z @kj that known limit is closed by `DEF-MARK-48`, and it was reachable the markdown way after all: python-markdown does keep the block raw, but a `<h3>` or a `<p>` written inside it is still a block child by the time BeautifulSoup reads it, so a callout with a heading in it took the unwrap path and lost its box
  - log: 2026-08-27T00:00:00Z @kj hardened, from the confirming review: the border test matched `prop.startswith('border')` and wrote a border off on three exact strings, so `border-collapse`, `border-spacing` and `border-image` - which open with the same word and draw no line - each took a full callout box, and so did `border: 0px`, `border: 0 none`, `border: none !important` and `border: 1px solid transparent`. A callout is a loud construct and the reader takes it as an aside the author marked out, so drawing one where the CSS draws nothing invents structure. The properties that draw a line are now written out as a set, and a declaration counts only when no token writes it off - `none`, `hidden`, `transparent`, or a width measuring zero in any unit, with `!important` stripped first. `border-bottom` still takes the box: it draws a line, and rendering any border as one left bar is the fidelity this defect already chose
  - log: 2026-08-27T00:00:00Z @kj tests: `TestHtmlCalloutBox`, eight regressions across HTML, DOCX and PDF. Mutation-proved on four reverts - the box pass off (4 fail, both formats), the black-bar guard restored (the black-bordered box loses its PDF box), the picture guard off (a picture is left outside its box), and the marker emitted in the text-holder branch too (a list item is swallowed by a box)
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [ ] `DEF-MARK-44` **a table-of-contents link to a heading resolves to nothing in DOCX and PDF** - MEDIUM; `04-sow-broilers-field-poc-delivery.md` writes JupyterLab-preview anchors (`#1.-Introduction`), python-markdown's `toc` extension slugs the heading to `1-introduction`, and `_SAFE_BOOKMARK_ID_RE` then refuses that slug outright because a Word bookmark name may not open with a digit - so the DOCX carries 16 `w:hyperlink w:anchor` elements against 8 bookmarks, none of them matching, and the reportlab rebuild carries no internal links at all; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered from the confirming review, which reported it against `DEF-MARK-42` - the TOC becoming a list of links is what made the dangling visible. Measured as pre-existing and independent of that change: a plain column-0 `[Intro](#1.-Introduction)` beside `## 1. Introduction` exports the same dead anchor on a document with no indented list in it, and the heading gets no `w:bookmarkStart` either way. Not fixed here: the remedy is a slug-equivalence pass over the anchor machinery plus a bookmark-name mangling scheme for headings that open with a number, which reaches every document, not the one the change touched
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-45` **a tab-indented line is not moved with the block around it** - MAJOR; `normalize_list_indentation` measures indentation in literal spaces, so `Intro.` / blank / `  - a` / `\t- nested` / `  - b` dedents `- a` and leaves the other two, and the item written as a sibling of `a` comes out as its child; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered from the confirming review. Not a regression - the same input previously exported as one literal source block, all three items and the structure lost. The reviewer's remedy (measure on `line.expandtabs(4)`) was refuted at the consumer two lines below: the shift is applied by slicing CHARACTERS, so a line measured at column 4 through one tab and sliced at index 2 becomes `' nested'` - a tab and a content character gone. The correct variant, rebuilding leading whitespace in columns, eats a tab inside a fenced body - a Makefile recipe in a sample inside an indented list - which is the same class of harm the fence ordering above was fixed for
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-MARK-46` **a bordered div holding inline math takes its box in DOCX and none in the PDF** - MAJOR; the PDF path replaces math with an `<img>` before `restructure_html_for_docx` runs, so `_mark_callout_box`'s picture guard fires on that path only, while the DOCX path sees OMML markers and boxes the div; one line of source, a callout in Word and no callout in the PDF; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered from the confirming review. Its remedy - exclude math images from the guard - was measured and does not close the construct: with the guard removed the equation still lands below the box and still at 528pt page width, because htmldocx gives any picture a paragraph of its own. That displacement is pre-existing and is the louder half of the defect; a box drawn around a sentence whose equation dangles beneath it is not the fix. The review also read the PDF's grey band in the background case as new - it is byte-identical to the pre-change build (`fill=(0.933, 0.933, 0.933) rect=[42.0, 70.8, 149.6, 85.2]` either way), the background-to-pill branch predating this work
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [x] `DEF-MARK-47` **a colour is read out of a file name and out of a custom property name** - MEDIUM; `_CSS_COLOR_TOKEN_RE` matched a bare `[a-z]+` run anywhere in a declaration value, so `background: url(assets/red-banner.png)` took a solid red callout box (bar and fill `FF0000`, red behind black text) and `background: var(--blue-500)` shaded the run `0000FF`; the browser paints neither, so HTML disagreed with DOCX and PDF on the same line of source; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered and fixed from the three-lens review, all three lenses agreeing. A regression of the shorthand scan added for `DEF-DIAG-29`: the code it replaced read `val.split()[0]` and could not be fooled this way, and widening `_CSS_NAMED_COLORS` from 26 names to the full 148 multiplied the surface - `tan`, `plum`, `linen`, `peru`, `snow`, `gold`, `salmon`, `wheat`, `azure`, `ivory` and `khaki` are all words that turn up in a file name
  - log: 2026-08-27T00:00:00Z @kj fixed by matching a function call whole and first: `[a-z-]*\([^()]*\)` swallows `url(...)` and `var(...)` before the bare-name alternative can reach inside them, and `rgb()` / `rgba()` are function calls too, so they still resolve as themselves. A bare name as the value is unaffected, and a colour written beside a `url()` in one shorthand (`#ffffff url(bg.png) no-repeat`) still resolves - it did before only because it stood first
  - log: 2026-08-27T00:00:00Z @kj tests: `TestCssColourInFunctionCall`, three regressions through the export API - the file name draws no box, the custom property shades no run, and the bare name, `rgb()` and the colour beside a `url()` all still arrive. Mutation-proved on the old pattern: reverted, the div is boxed `FF0000` and the run shaded `0000FF`
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-48` **a bordered div holding block children takes no box** - MEDIUM; the same `<div style="border: 2px dashed #9ca3af; background: rgba(244, 244, 245, 0.45)">` boxed in DOCX and PDF when it held a line of text and exported as plain paragraphs the moment an `<h3>` was written inside it; `_mark_callout_box` was called only from the no-block-children branch of the div pass, and the block-children branch unwrapped the div and dropped its style; the HTML export boxes both spellings, so one construct had two answers; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered and fixed from the three-lens review. This is the limit `DEF-MARK-43` recorded as known and left open; the shape that makes it common is a callout with a heading in it, which is not an exotic spelling
  - log: 2026-08-27T00:00:00Z @kj fixed by marking the blocks rather than the div: the marker cannot ride on the div, which is unwrapped, and a bare text node left at body level joins whatever paragraph stood ABOVE it - measured, so a marker placed there would box the paragraph before the callout. The first block child carries the `⁣BOX:<bar>:<fill>⁣` marker and the rest carry `⁣BOX+⁣`, and `style_docx_alert_boxes` puts the whole run of paragraphs into one table: a table each would draw a stack of boxes with a spacer between them where the author drew one aside. The continuation marker is what keeps two adjacent divs in the same colours two boxes
  - log: 2026-08-27T00:00:00Z @kj boundaries, measured rather than assumed: only `<p>` and `<h1>`-`<h6>` children take the box, because a marker put in one lands in exactly one body paragraph. A div holding a table or a code block takes no box - htmldocx gives those a structure of their own that would stay outside the box the way a picture does - and neither does one holding a list, which arrives as one paragraph per item and which the PDF rebuild redraws as callout body text with the bullet gone. The `DEF-MARK-43` placement rule is unchanged: never inside a `<li>`, a `<td>` or a heading, and never around a picture
  - log: 2026-08-27T00:00:00Z @kj tests: `TestHtmlCalloutBoxWithBlocks`, six regressions across HTML, DOCX and PDF. Mutation-proved twice - the marking off (3 fail, both binary formats), and a full box marker on every block instead of the continuation (the two-paragraph div draws 2 boxes where the author drew 1)
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-49` **a border latched on could not be switched off, and a colour alone was read as one** - MEDIUM; `_css_callout_box` set `bordered` True on the first border declaration that drew a line and never cleared it, so `border: 1px solid #ccc; border-style: none` took a full callout box the browser draws nothing of, and `border-color: #ccc` on its own took one too though `border-style` is `none` until something says otherwise; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered and fixed from the three-lens review, which named the precedent: `DEF-DIAG-39` fixed exactly this class for emphasis by folding declarations per property with the last value winning
  - log: 2026-08-27T00:00:00Z @kj fixed by reading the border as the cascade reads it. Each declaration is folded onto the side and the part it names - `border`, `border-left`, `border-style`, `border-bottom-color` and the rest - and a shorthand resets all three parts of its sides, so what it leaves out returns to its initial value instead of keeping what a declaration above gave it. A side draws only once it has a style, which is what keeps a colour on its own from being a border. `_css_border_draws` is gone, its three write-off spellings now read per side: no style or one drawing nothing, a width measuring zero in any unit, and the colour `transparent`
  - log: 2026-08-27T00:00:00Z @kj the round-2 behaviour is unchanged and still tested: a shorthand, a bare `border-style` and a one-sided border each take the box, while `0px`, `none !important`, `border-image`, `border-collapse` and a transparent colour take none
  - log: 2026-08-27T00:00:00Z @kj tests: `TestCssBorderCascade`, three regressions - the longhand switching a shorthand off, the colour alone, and the last declaration winning in the other direction. Mutation-proved on the latched loop restored: the switched-off border and the bare colour each draw a box again
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-50` **an unresolvable colour declaration still reached htmldocx** - MEDIUM; the guard written for `DEF-DIAG-29` was keyed on the single spelling `rgba?\(`, but `_normalize_css_color` answers nothing for any hex whose length is not 3 or 6, and htmldocx then reads a channel with `int()` and slices a hex blind; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered from the three-lens review and re-measured before fixing, because the reported example does not fail the reported way: `color: #12345` returns HTTP 200 and paints the run `123405` - htmldocx slices the first two pairs and a nibble - which is a colour on no screen and one a browser never shows, an invalid declaration being ignored outright. The 500s are `#1234`, `#12` and `#gggggg` (`invalid literal for int() with base 16: ''`) and, on the `color` property, `var(--rgb-blue)` (`invalid literal for int() with base 10: '---'`), htmldocx routing a value on the bare substring `rgb`. HTML returns 200 for all of them, so the whole document was lost in two formats out of three
  - log: 2026-08-27T00:00:00Z @kj fixed by dropping any colour declaration that resolved to nothing, whatever its notation, rather than guarding one notation. Nothing is lost by it: htmldocx understands a `#` hex and the `rgb` substring and answers black for everything else, so a value this code cannot resolve has no better reading there. The run keeps the body colour, which is what a browser leaves when it cannot parse a declaration. `_CSS_RGB_ANY_RE` is gone with the guard
  - log: 2026-08-27T00:00:00Z @kj tests: `TestUnresolvableColourValue`, three regressions - the malformed hex and the `rgb`-named custom property each keep all three formats at 200 with the document's text intact, and the five-digit hex leaves the run unpainted. Mutation-proved on the old single-notation guard restored: two 500s and a run painted `123405`
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-51` **loose text inside a boxed div is welded into the block above it** - MEDIUM; `<h3>Heads up</h3>`, a bare line of text and `<p>a paragraph</p>` written inside one bordered div exported as two paragraphs, the first of them reading `Heads uploose text between` in Heading 3: the block-children branch marks only what `div.find_all(_HTML_BLOCK_TAGS, recursive=False)` returns, a bare text node carries no marker, so it stayed at body level when the div was unwrapped and htmldocx joined it to the paragraph above - three authored blocks arriving as two, one of them in a style the author never wrote and with no space where the join happened; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered and reproduced from the three-lens review against `DEF-MARK-48`, which is what made the branch reachable: before it, a div holding block children took no box at all
  - log: 2026-08-27T00:00:00Z @kj fixed: `_boxed_blocks` gathers each run of loose inline content between two block children into a `<p>` of its own before the markers go in, which is the anonymous block box a browser gives that text. Consecutive inline nodes share one paragraph rather than taking one each - `loose <b>bold</b> tail` is one line, not three - and a run holding nothing but the newlines between two tags is left where it is instead of boxing a blank line. The line breaks the author wrote around the text are trimmed at the edges of the new paragraph, where a browser drops them and Word would show a leading space
  - log: 2026-08-27T00:00:00Z @kj ordering, measured: the wrap runs AFTER the boxable-children guard, which asks what the author wrote in the div - a paragraph this pass makes is boxable by construction and would answer for a table that is not. `_mark_callout_box` now returns the blocks it boxed, and the div hands its alignment and its colour to those rather than to the children it was read with, so what the pass creates is inside the box like everything else
  - log: 2026-08-27T00:00:00Z @kj tests: `TestCalloutBoxContents`, four regressions with `DEF-MARK-52` and `DEF-MARK-53`. Mutation-proved on the wrap reverted: `the box holds ['Heads uploose text between', 'a paragraph']; the three blocks the author wrote must arrive as three, in the order written`
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-52` **a heading inside a callout box reads as body text in the PDF** - MEDIUM; `process_alert` painted every paragraph of the callout cell with `callout_body_style` and never read the paragraph style, so the `Heading 3` the DOCX keeps came out at 12.0pt in the body face - the same size as the body text beside it in the same box, against the heading Word shows; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered from the three-lens review against `DEF-MARK-48`. It pre-dates that change: nothing used to route a heading into a callout cell, so `DEF-MARK-48` is what made it reachable rather than what broke it
  - log: 2026-08-27T00:00:00Z @kj fixed by reading the one ladder instead of inventing a second: the heading mapping in `process_paragraph` - level, the catch-all for an unnumbered `Heading ...`, and the upright-face fallback for a minor heading whose italic face has no glyph for the text - is now `heading_style_for`, and `process_alert` asks it per paragraph. The heading colour and size come with it, so the box shows what the body would show at that level, and Word's heading style says the same
  - log: 2026-08-27T00:00:00Z @kj measured: the boxed `<h3>` draws at 13.2pt in the heading face where it drew at 12.0pt in the body face, against 12.0pt body text in the same box. The style's `spaceBefore` is dropped on the first flowable only - the cell's own top padding is the space above the first line, and the heading's would sit on top of it
  - log: 2026-08-27T00:00:00Z @kj tests: `test_pdf_agrees_that_a_boxed_heading_is_a_heading` reads the DOCX heading and the PDF span for one document, so neither format can move alone. Mutation-proved on `callout_body_style` restored: `the boxed heading is 12.0pt against 12.0pt of body text in the same box - the PDF dropped the heading level Word keeps`
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-53` **the colour declared beside a box's fill reached neither binary format** - MEDIUM; `<div style="border: 2px solid #333; background: #eeeeee; color: #ff0000">` painted the fill and dropped the colour on both spellings of the construct: a div holding blocks is unwrapped and its style attribute goes with it, and a childless one keeps the declaration on the paragraph it becomes, where htmldocx never looks - it reads a colour off an inline tag only. A browser paints both, so making a background load-bearing on a div - it now fills a whole cell - gave an author who declared the pair one of it; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj registered and reproduced from the three-lens review against `DEF-MARK-48`; measured `colours=set()` on every run of the box in the DOCX and `000000` on every span of it in the PDF
  - log: 2026-08-27T00:00:00Z @kj fixed: `_carry_box_color` puts the div's own `color` on a `<span>` around each boxed paragraph's contents - around the contents rather than on the block, which is the placement the emphasis pass already uses for a block, and the reason a colour left on the paragraph arrived nowhere. Both spellings go through it, so the two answer alike, and the PDF needs nothing: it rebuilds its runs from the finished DOCX
  - log: 2026-08-27T00:00:00Z @kj a heading in the box is left out, deliberately: the stylesheet gives a heading a colour of its own and that beats what it inherits, so a browser draws the heading inside a red-texted box blue - and Word's heading style and the PDF ladder draw it blue too. Colouring it would have been the one place the three formats disagreed
  - log: 2026-08-27T00:00:00Z @kj contrast left with the author, and this is the pairing question the box raises: the fill and the colour come from one declaration block, and the fill Word and the PDF paint is the colour the browser composites, so the pairing the reader sees in the HTML export is the pairing they get in the other two. Overruling either value to force legibility is the only move that would make the formats differ. A colour written on a `<span>` has always been carried verbatim; the box is now carried the same way rather than under a legibility rule of its own
  - log: 2026-08-27T00:00:00Z @kj ground truth: the six hand-drawn callouts of `04-sow-broilers-field-poc-delivery.md` declare `color: #000000`, and across the whole document the only change is 30 runs going from no explicit colour to an explicit `000000` - the author's own declaration arriving. Box count, bars, fills, paragraph styles, run text, bold and every PDF span of the callouts are identical either side of the change
  - log: 2026-08-27T00:00:00Z @kj tests: `test_the_box_colour_reaches_the_text_it_was_declared_with` asserts the paint in DOCX and PDF and that the heading is not painted with it, and `test_a_box_around_one_paragraph_carries_its_colour_too` covers the childless spelling. Mutation-proved on both call sites reverted: `the loose text is painted set(); the colour declared beside the fill reached neither format` and `the colour declared on the box reached no run`
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-58` **a heading written inside a list item ended the list** - MAJOR; an author's `### Sub` at an item's own content column matched the chunker's ATX test, which cleared the open-list flag without reading the heading's indent, so the item's child list was judged standalone and dedented into the numbering above - `1. Step one` / `### Sub-heading` / `- detail` / `2. Step two` exported with the bullet as step 2 and the author's step 2 as step 3; fix: only a column-0 heading ends a list; `routes.py`
  - repro: write a heading indented under a numbered step, with a sub-list below it, and export to Word
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the sixth adversarial round, two lenses converging on 58 and 59; regression test in TestIndentedTopLevelList, mutation-proved; 322 pytest green
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-59` **a list at column 0 left the previous list's column standing** - MAJOR; the loose-list continuation carry (`same_col`) was cleared only by a column-0 chunk with no marker, so a new list starting at column 0 left the earlier indented list's column in place and any later chunk sitting at that column was shifted by it - a genuinely nested sub-list under `- top` was flattened to a sibling, and the ordered spelling lost its numbering entirely; fix: a list written at column 0 clears the carry; `routes.py`
  - repro: an indented two-line index, then a column-0 list with a nested child below it
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the sixth adversarial round, two lenses converging on 58 and 59; regression test in TestIndentedTopLevelList, mutation-proved; 322 pytest green
  - log: 2026-08-27T00:00:00Z @kj widened: the third lens of the same round found the first fix too narrow - it cleared the carry only for a list at column 0, so a second list written one column in still left the first list's column standing and tore that second list's own child out from under it. The carry now ends on any chunk whose outermost marker does not stand at the carried column
  - log: 2026-08-27T00:00:00Z @kj superseded by DEF-MARK-63: the carried column is gone. A loose list is now gathered whole before anything moves, so there is no state to outlive its list; the shape this defect names is refused outright and its test asserts the refusal
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-60` **a shift of one column re-levelled a ragged child list** - MAJOR; the rescue window opened at one column in, but `OListProcessor.RE` already allows `tab_length - 1` leading spaces, so a marker one column in needs no rescue - and since the nesting level is the column floor-divided by the tab length, the odd shift moved a ragged child off the grid and collapsed two levels into one; ` - Alpha` / `  - child` / ` - Beta` rendered as three siblings where the control nested them correctly; fix: the window floor is two; `routes.py`
  - repro: a list one space in with a child two spaces in, exported to any format
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the sixth adversarial round, two lenses converging on 58 and 59; regression test in TestIndentedTopLevelList, mutation-proved; 322 pytest green
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-61` **a rescued index swallowed the numbered list under it** - MAJOR; python-markdown reads two lists separated by nothing but a blank line as one, so an index rescued to column 0 directly above a column-0 numbered list took its steps as bullets - the control left the index as literal source and kept the numbering, which is the better trade; fix: decline the rescue when the next chunk opens a column-0 list of the other kind with no heading or paragraph parting them; `routes.py`
  - repro: an indented two-line index, a blank line, then `1.` / `2.` at column 0
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the sixth adversarial round, two lenses converging on 58 and 59; regression test in TestIndentedTopLevelList, mutation-proved; 322 pytest green
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-62` **a table under an indented list became literal pipes** - MAJOR; a column-0 line was allowed to stand while the markers around it moved - it is already where the shift would put it - but the arriving list makes it a lazy continuation, so a table written directly under an indented list came out as pipe characters inside a list item where the control drew a real table, and a `===` underline turned the item above it into a heading carrying its own `- `; fix: refuse the rescue when such a line is a table row or a setext underline; `routes.py`
  - repro: an indented list with a pipe table on the very next line, no blank line between
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the sixth adversarial round, two lenses converging on 58 and 59; regression test in TestIndentedTopLevelList, mutation-proved; 322 pytest green
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-63` **a loose list was rescued half at a time** - MAJOR; the continuation shift reached `shift_chunk` above every guard, so one blank line between two index items bought an unguarded move; and even guarded, refusing a later chunk left the list HALF moved - the part at column 0 became a list and the part left behind became its child, dragging in the table or the numbered procedure below it; fix: a loose list is gathered whole before anything moves and one refusal stands for all of its chunks, which retires the carried-column state entirely; `routes.py`
  - repro: an index written two spaces in with a blank line between its two items, and a table or a numbered list directly under it
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj edited text
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the seventh adversarial round, three lenses converging; regression test in TestIndentedTopLevelList, mutation-proved; 326 pytest green, 8,924-file differential 30 changed / 30 better / 0 worse
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-64` **the swallow test knew one setext spelling and one table spelling** - MAJOR; the guard read the `=` setext underline but not `-`, the level-2 spelling and the commoner of the two, and required a table row to open with a pipe, which neither python-markdown tables nor GitHub asks for - so a `---` underlined heading and a borderless table were both left standing to be swallowed as the arriving list lazy continuation; fix: both setext spellings, and any line carrying an unescaped pipe; `routes.py`
  - repro: an indented index with a `My Section` / `---` heading pair under it, or with a borderless `Name | Age` table
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj edited text
  - log: 2026-08-27T00:00:00Z @kj edited title
  - log: 2026-08-27T00:00:00Z @kj edited text
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the seventh adversarial round, three lenses converging; regression test in TestIndentedTopLevelList, mutation-proved; 326 pytest green, 8,924-file differential 30 changed / 30 better / 0 worse
  - log: 2026-08-27T00:00:00Z @kj edited repro (replaced)
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-65` **two adjacent lists of the same kind merged unguarded** - MAJOR; the refusal compared marker kinds and declined only for the other kind, but python-markdown merges two adjacent ordered lists as readily as a mixed pair and renumbers the second - a rescued `1. Overview` / `2. Method` index turned the author’s step 1 into step 3; fix: decline for a column-0 list of either kind, which is also less code; `routes.py`
  - repro: an ordered index two spaces in, a blank line, then a numbered procedure at column 0
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj edited text
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the seventh adversarial round, three lenses converging; regression test in TestIndentedTopLevelList, mutation-proved; 326 pytest green, 8,924-file differential 30 changed / 30 better / 0 worse
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-66` **an indented heading counted as parting two lists** - MAJOR; the parting test accepted any heading between two chunks, but a break line is never moved by the shift, so an indented heading ends up inside the rescued item and parts nothing - while having already disabled the guard that keeps the numbered list below from being absorbed; fix: only a heading at column 0 parts; `routes.py`
  - repro: an indented index, a heading indented three spaces, then a numbered list at column 0
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj edited text
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the seventh adversarial round, three lenses converging; regression test in TestIndentedTopLevelList, mutation-proved; 326 pytest green, 8,924-file differential 30 changed / 30 better / 0 worse
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-MARK-67` **a fenced sample was rewritten from inside a gathered list** - MAJOR; the run gathering judged a chunk by its list markers alone, so a chunk holding a fenced block whose sample text carried markers at the run column joined the run and `shift_chunk` stripped two columns from inside the sample - `services:` with its items at column 2 exported at column 0, which is different YAML; found by exhaustive four-line fuzzing, 108 documents mutated a line the fence tracker had marked verbatim; fix: the run gathering is gone with DEF-MARK-68, so the pass only ever moves one chunk, and a chunk holding a fence or a raw `<pre>` never moves - a sample keeps its own columns under two rules; `routes.py`
  - repro: an indented list, a blank line, a fenced yaml sample whose own body is an indented list
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj edited text
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the eighth adversarial round, all three lenses DO-NOT-SHIP with four CRITICALs between them; the pass now moves one chunk and only when what follows certainly ends the list. Regression tests in TestIndentedTopLevelList, mutation-proved; 328 pytest green
- [x] `DEF-MARK-68` **the refusal read one neighbouring chunk and only its first line** - MAJOR; what a rescued list absorbs when it lands is not knowable from the next chunk first line: an indented note between the list and a numbered procedure hid that procedure from the guard, a list starting on a later line of the next chunk was invisible to it, and a list one column in passed a test written for column 0 although `OListProcessor.RE` reads one leading space as top level - each destroyed the numbering the author wrote, in all three formats, and a four-line fuzz put it at 450 of 24,591 generated documents; fix: the rescue is taken only when what follows certainly ENDS the list - a column-0 heading, column-0 prose, or the end of the document; `routes.py`
  - repro: an index two spaces in, an indented note under it, then a numbered procedure at column 0
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the eighth adversarial round, all three lenses DO-NOT-SHIP with four CRITICALs between them; the pass now moves one chunk and only when what follows certainly ends the list. Regression tests in TestIndentedTopLevelList, mutation-proved; 328 pytest green
- [x] `DEF-MARK-69` **a rule inside the rescued chunk became a heading underline** - MEDIUM; a `---` or `===` line standing at the shift column was never examined - the body scan reads only lines LEFT of the shift - so once the chunk landed at column 0 the rule underlined the line above it, exporting a Word `Heading 2` whose text carries the raw `- ` marker into the navigation pane, the TOC field and the generated index, while the rule the author wrote disappeared; fix: a chunk holding a bare `---` or `===` line is refused; `routes.py`
  - repro: an indented list with a `---` line between two of its items, followed by prose
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the ninth adversarial round, the first to return a SHIP verdict from any lens; regression test in TestIndentedTopLevelList, mutation-proved; 331 pytest green
- [x] `DEF-MARK-70` **the raw pre tracker closed a block that was still open** - MEDIUM; the scanner asked whether a line carried a closing tag rather than counting the tags on it, so `<pre>x</pre><pre>` and a nested `<pre>` both left it believing no block was open - the lines that followed were not marked verbatim and the pass rewrote indentation inside a still-open preformatted block, the one thing DEF-MARK-67 promises never happens; fix: a depth, counted per line, not a flag; `routes.py`
  - repro: a `<pre>x</pre><pre>` line, then an indented list, then `</pre>`
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the ninth adversarial round, the first to return a SHIP verdict from any lens; regression test in TestIndentedTopLevelList, mutation-proved; 331 pytest green
- [x] `DEF-MARK-71` **an indented heading after the list was invisible to the refusal** - MAJOR; the chunker records every ATX line as a BREAK rather than a chunk, and only a column-0 break parts two lists, so a heading indented one to three columns was neither something the refusal could read nor something that parted anything - and `shift_chunk` does not move a break, so it stayed indented under a list now at column 0, became that item child block and took whatever was welded to it along, while the heading stopped being a heading in every format; fix: an indented heading standing after the chunk refuses the rescue outright; `routes.py`
  - repro: an index two spaces in, then `  ## Sub` indented two spaces, then a setext title
  - test-tags: unit
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the ninth adversarial round, the first to return a SHIP verdict from any lens; regression test in TestIndentedTopLevelList, mutation-proved; 331 pytest green
- [x] `DEF-MARK-72` **a galata assertion could not fail on the regression it named** - MINOR; the two `not.toContain("](#")` guards in the new galata spec were vacuous: codehilite tokenises an indented code block into spans, so the literal source arrives split across element boundaries - `<span class="p">](</span><span class="err">#</span>` in HTML and one `<w:t>` run per span in Word - and the substring is absent whether or not the pass ran; fix: dropped, the sibling assertions on the rendered list items and the anchor links do discriminate; `ui-tests/tests/export-fidelity.spec.ts`
  - repro: bypass the pass and run the galata Word test - the assertion still passes
  - test-tags: e2e
  - log: 2026-08-27T00:00:00Z @kj added
  - log: 2026-08-27T00:00:00Z @kj closed: fixed: found by the ninth adversarial round, the first to return a SHIP verdict from any lens; regression test in TestIndentedTopLevelList, mutation-proved; 331 pytest green
- [x] `DEF-MARK-73` **an indented heading behind the list was invisible too** - MAJOR; DEF-MARK-71 closed only the forward half: an ATX line is a break rather than a chunk, so a column-0 chunk standing right after an INDENTED heading is not a block of its own but a lazy continuation of the item that heading sits in, and the list above is still open - reading it as the end of the list let a child list below be rescued out from under its parent and merged into the bullet list above, losing both the nesting and the numbering; a five-line exhaustive differential over 2,476,099 documents put it at 72 harmed, all of them this one shape; fix: a chunk that follows an indented heading does not end the list; `routes.py`
  - repro: a bullet item, an indented `### Notes`, a column-0 line, then an indented numbered sub-list
  - test-tags: unit
  - log: 2026-08-27T14:30:23Z @kj added
  - log: 2026-08-27T14:32:09Z @kj closed: fixed: found by the ninth round's architect lens, the mirror of DEF-MARK-71; regression test in TestIndentedTopLevelList, mutation-proved; 332 pytest green, 8,921-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-74` **the fence tracker used CommonMark rules, the converter does not** - MAJOR; the scanner matched a fence at any indent and closed on a run at least as long, but `FencedBlockPreprocessor.FENCED_BLOCK_RE` anchors the closer after a newline and matches it with a backreference - so an indented closer or a longer inner fence closed the pass block while the converter kept its own open, and `shift_chunk` then rewrote bytes inside a real code sample; two lenses converged, one enumerating 823,543 documents and finding 84 harmed, the other reproducing it end to end through the export endpoints, and a scan of 8,502 workspace files found 19 already carrying the precondition; fix: remember the opening fence column and close only on the same character, the same length and the same column; `routes.py`
  - repro: a yaml fence, a filler line, an indented ``` closer, then an indented list
  - test-tags: unit
  - log: 2026-08-27T15:07:24Z @kj added
  - log: 2026-08-27T15:08:31Z @kj closed: fixed: found by the tenth adversarial round, three lenses converging on the fence-close rule and on tab measurement; regression test in TestIndentedTopLevelList, mutation-proved; 337 pytest green, 8,934-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-75` **a tab measured as no indentation at all** - MAJOR; every column in the pass is counted with `len(ln) - len(ln.lstrip(" "))`, which sees spaces only, while python-markdown runs `expandtabs(tab_length)` before parsing - so a tab-indented successor certified as column-0 prose that ends the list, the rescue was taken, and the converter then read that line as a child of the list which had just arrived at column 0 and took what followed it: in one spelling a numbered procedure came out as more bullets of the index, in another a `---` became the setext underline of a fabricated Word Heading 2 carrying the raw `- ` marker; fix: a chunk or a successor indented with a tab is refused rather than measured; `routes.py`
  - repro: an indented index, a blank line, then a tab-indented note, then a numbered procedure at column 0
  - test-tags: unit
  - log: 2026-08-27T15:07:24Z @kj added
  - log: 2026-08-27T15:08:31Z @kj closed: fixed: found by the tenth adversarial round, three lenses converging on the fence-close rule and on tab measurement; regression test in TestIndentedTopLevelList, mutation-proved; 337 pytest green, 8,934-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-76` **a rescued ordered index restarted at one** - MINOR; `OListProcessor.LAZY_OL` is on and this project never turns it off, so the `start` attribute is never written and a rescued list renders from 1 whatever the author numbered it - an index opening at `3.` exported as `1.` in all three formats, pointing the reader at the wrong section, where the control at least showed the literal `3.`; fix: an ordered chunk whose first marker is not `1` is refused; `routes.py`
  - repro: an index two spaces in whose first item is numbered 3
  - test-tags: unit
  - log: 2026-08-27T15:07:24Z @kj added
  - log: 2026-08-27T15:08:31Z @kj closed: fixed: found by the tenth adversarial round, three lenses converging on the fence-close rule and on tab measurement; regression test in TestIndentedTopLevelList, mutation-proved; 337 pytest green, 8,934-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-77` **the inert-comment exception waved through the lines behind it** - MINOR; an HTML comment at column 0 was allowed to stand while the chunk moved, on the premise that it renders as nothing - true of the comment, false of what the shift then did to the rest of the chunk: an indented code sample below the comment was shifted to column 0, stopped being a code block and rendered as prose with its own child indentation collapsed, which for the yaml in the reported shape makes `web: nginx` a sibling of `services:` rather than its child; fix: the exception applies only when the comment is the last line of the chunk, which is what a generated index end marker is; `routes.py`
  - repro: an indented index, an HTML comment, then an indented yaml sample, all in one chunk
  - test-tags: unit
  - log: 2026-08-27T15:07:24Z @kj added
  - log: 2026-08-27T15:08:31Z @kj closed: fixed: found by the tenth adversarial round, three lenses converging on the fence-close rule and on tab measurement; regression test in TestIndentedTopLevelList, mutation-proved; 337 pytest green, 8,934-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-78` **a closing pre tag inside a comment closed the block** - MINOR; the raw `<pre>` depth counted tags textually, so a `</pre>` written inside an HTML comment took the depth to zero while the converter own raw-HTML extractor kept the block open, and the pass rewrote indentation inside it; fix: comments are stripped before the tags on a line are counted; `routes.py`
  - repro: a `<pre>` opener, a `<!-- </pre> -->` line, then an indented list, then the real closer
  - test-tags: unit
  - log: 2026-08-27T15:07:24Z @kj added
  - log: 2026-08-27T15:08:31Z @kj closed: fixed: found by the tenth adversarial round, three lenses converging on the fence-close rule and on tab measurement; regression test in TestIndentedTopLevelList, mutation-proved; 337 pytest green, 8,934-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-79` **tab-led chunk clears the open list** - MAJOR; a tab-indented continuation paragraph measured as column 0 and cleared after_list, so the child list under it was rescued as standalone and hoisted into the parent numbering in all three formats; cause: third site of the DEF-MARK-75 miscount, the chunk classifier used lstrip(" "); fix: lstrip(" \t") at the classifier; `routes.py`
  - repro: export "1. Step one" + blank + tab-indented paragraph + blank + " - detail a"; the bullets become steps 2 and 3
  - test-tags: unit
  - log: 2026-08-27T17:09:02Z @kj added
  - log: 2026-08-27T17:09:02Z @kj closed: fixed: found by the eleventh adversarial round (bug-hunter lens); regression test in TestIndentedTopLevelList, mutation-proved; 340 pytest green, galata 5/5, 8,937-file differential 16 changed / 16 better / 0 worse
  - log: 2026-08-27T17:09:16Z @kj correction: the round-11 finder was the architect lens, not bug-hunter
- [x] `DEF-MARK-80` **NBSP-only line read as the blank that ends a chunk** - MEDIUM; str.strip() empties a line holding NBSP or form feed; NormalizeWhitespace empties only space-only lines, so the converter read the next line as a lazy continuation of the rescued list, welding a setext heading or a paragraph into the last bullet; fix: one blank() helper with strip(" \t\r") at every blank-judging site of the pass; `routes.py`
  - repro: export " - a", a line holding only U+00A0, "Text.", "---"; the Heading 2 vanishes into the bullet
  - test-tags: unit
  - log: 2026-08-27T17:09:02Z @kj added
  - log: 2026-08-27T17:09:02Z @kj closed: fixed: found by the eleventh adversarial round (bug-hunter lens); regression test in TestIndentedTopLevelList, mutation-proved; 340 pytest green, galata 5/5, 8,937-file differential 16 changed / 16 better / 0 worse
  - log: 2026-08-27T17:09:16Z @kj correction: the round-11 finder was the architect lens, not bug-hunter
- [x] `DEF-MARK-81` **heading with no space after the hashes escapes the chunker** - MINOR; atx_re required whitespace after the hashes but HashHeaderProcessor.RE does not, so "#Note" inside an indented chunk recorded no break and became a fabricated heading once shifted to column 0, splitting the index and renumbering its second half from 1; fix: atx_re matches what the converter matches; `routes.py`
  - repro: export " 1. one" / " #Note" / " 2. two"; a Heading 1 appears and "2." renders as "1."
  - test-tags: unit
  - log: 2026-08-27T17:09:02Z @kj added
  - log: 2026-08-27T17:09:02Z @kj closed: fixed: found by the eleventh adversarial round (bug-hunter lens); regression test in TestIndentedTopLevelList, mutation-proved; 340 pytest green, galata 5/5, 8,937-file differential 16 changed / 16 better / 0 worse
  - log: 2026-08-27T17:09:16Z @kj correction: the round-11 finder was the architect lens, not bug-hunter
- [x] `DEF-MARK-82` **interior line at the shift column splits the rescued list** - MINOR; a raw HTML block, a thematic break or a comment at the shift column is content while the chunk is a code block and its own block once the chunk lands at column 0, so the rescued index split in two and its tail renumbered from 1; cause: the refusal predicted line by line what the converter would do; fix: the pass now renders the document with and without the shift and keeps it only when exactly one block changed, from code to list; `routes.py`
  - repro: export " 1. First" / " <div>note</div>" / " 2. Second"; section 2 renders as 1.
  - test-tags: unit
  - log: 2026-08-27T17:37:14Z @kj added
  - log: 2026-08-27T17:37:14Z @kj closed: fixed: round 11 (bug-hunter, ux-designer and own audit); regression test in TestIndentedTopLevelList, mutation-proved against the rendered-document check; 343 pytest green, 8,949-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-83` **whitespace-only successor the converter erases does not end the list** - MAJOR; a column-0 line holding only an NBSP or form feed was read as prose that certainly ends the rescued list, but the converter renders it as nothing, so the list stayed open and the numbered procedure below became items 3 and 4 of the index; cause: the same blank-line model as DEF-MARK-80 at the successor site; fix: covered by the rendered-document check, the merged list is a second changed block; `routes.py`
  - repro: export " 1. Overview" / " 2. Method", blank, a U+00A0 line, blank, "1. First step" / "2. Second step"; the steps render as 3 and 4
  - test-tags: unit
  - log: 2026-08-27T17:37:14Z @kj added
  - log: 2026-08-27T17:37:14Z @kj closed: fixed: round 11 (bug-hunter, ux-designer and own audit); regression test in TestIndentedTopLevelList, mutation-proved against the rendered-document check; 343 pytest green, 8,949-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-84` **1) read as a list marker the converter does not read** - MINOR; the pass shared the mermaid helper marker regex, which accepts "1)" and a marker with nothing after it; OListProcessor.RE accepts "1." only and both processors want a space after the marker, so an indented "1) one" sample was shifted to column 0 and rendered as a paragraph with its line breaks gone; fix: the pass has its own marker regex mirroring the converter; `routes.py`
  - repro: export " 1) one" / " 2) two"; the two lines run together as one paragraph
  - test-tags: unit
  - log: 2026-08-27T17:37:14Z @kj added
  - log: 2026-08-27T17:37:14Z @kj closed: fixed: round 11 (bug-hunter, ux-designer and own audit); regression test in TestIndentedTopLevelList, mutation-proved against the rendered-document check; 343 pytest green, 8,949-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-85` **indent pass measured the source, not the text the converter gets** - MAJOR; the pass ran before the task-list, alert and math passes, so it certified "- $$a = b$$" as a list item and the math pass then left a bare "- " line - the setext underline of the item above, exported as a Word Heading 2 carrying the raw marker; fix: the pass runs after every source-rewriting pass and before the format own code-block handling in all three handlers; `routes.py`
  - repro: export " - intro item" / " - $$a = b$$" / " - outro item" to DOCX; "- intro item" arrives as Heading 2
  - test-tags: unit, integration
  - log: 2026-08-27T18:20:17Z @kj added
  - log: 2026-08-27T18:24:07Z @kj closed: fixed: found by round 12 (ux-designer, architect, bug-hunter); regression test mutation-proved; 348 pytest green, galata 5/5, 8,950-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-86` **rescued-list block test was a six-tag copy of the converter set** - MINOR; own_block_re listed h1-h6/pre/table/hr/blockquote/div, so a <style> or <script> at the item content column passed the rescue and went live in the HTML export where the control showed escaped text in a code block; fix: the check asks md.is_block_level for every tag in the rescued block, excepting ul/ol/li; `routes.py`
  - repro: export " - a" / " <style>body{display:none}</style>" / " - b" to HTML; the page is blank
  - test-tags: unit
  - log: 2026-08-27T18:20:17Z @kj added
  - log: 2026-08-27T18:24:07Z @kj closed: fixed: found by round 12 (ux-designer, architect, bug-hunter); regression test mutation-proved; 348 pytest green, galata 5/5, 8,950-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-87` **a border width that is not a positive length still draws a callout** - MINOR; "border: -1px solid red" failed \_CSS_LENGTH_RE and was filed as a colour, "00px" passed it but not the zero test, so both sides counted as bordered and DOCX and PDF drew a box the browser and the HTML export do not; fix: the width is parsed numerically and a length at or below zero writes the side off; `routes.py`
  - repro: export a div styled "border: -1px solid red" to DOCX; a bordered table appears
  - test-tags: unit
  - log: 2026-08-27T18:20:17Z @kj added
  - log: 2026-08-27T18:24:08Z @kj closed: fixed: found by round 12 (ux-designer, architect, bug-hunter); regression test mutation-proved; 348 pytest green, galata 5/5, 8,950-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-88` **nested ordered run not opening at 1 is renumbered** - MINOR; the start-at-one rule read only the chunk first marker; a nested ordered run opening at "3." is a new list to the converter and LAZY_OL renders it from 1, so the author numbers vanish where the control showed them; fix: the rule applies to every ordered run at every depth in the chunk; `routes.py`
  - repro: export " 1. a" / " 3. b" / " 4. c" / " 2. d"; the PDF shows 1. b / 2. c
  - test-tags: unit
  - log: 2026-08-27T18:20:17Z @kj added
  - log: 2026-08-27T18:24:08Z @kj closed: fixed: found by round 12 (ux-designer, architect, bug-hunter); regression test mutation-proved; 348 pytest green, galata 5/5, 8,950-file differential 16 changed / 16 better / 0 worse
- [x] `DEF-MARK-89` **raw element in a rescued item goes live in the HTML export** - MINOR; is_block_level is the converter opinion about layout, not about what a browser acts on: <link>, <meta>, <base> and <img onerror> answer not-block-level, so a chunk holding one was rescued and the element the control escapes went live - a blank or hijacked page; fix: a chunk holding a raw HTML element is never a candidate (a comment is not an element and still closes a generated index); `routes.py`
  - repro: export " - a" / " <link rel=stylesheet href=data:text/css,body{display:none}>" / " - b" to HTML; the page is blank
  - test-tags: unit
  - log: 2026-08-27T19:02:20Z @kj added
  - log: 2026-08-27T19:09:29Z @kj closed: fixed: found by round 13 (architect, ux-designer, bug-hunter); regression test mutation-proved; 354 pytest green, galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
- [x] `DEF-MARK-90` **a candidate the converter cannot render took the export down** - MINOR; a bullet list nested 240 levels deep renders as a code block in the control and raises RecursionError inside the measurement of the shifted text, returned as HTTP 500 in all three formats; fix: a candidate the converter cannot render is a refusal; `routes.py`
  - repro: export a two-space list nested 240 levels; 500 maximum recursion depth exceeded
  - test-tags: unit
  - log: 2026-08-27T19:02:20Z @kj added
  - log: 2026-08-27T19:09:29Z @kj closed: fixed: found by round 13 (architect, ux-designer, bug-hunter); regression test mutation-proved; 354 pytest green, galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
- [x] `DEF-MARK-91` **markers of both kinds at one depth merge into one list** - MINOR; the start-at-one walk recorded depth only, so "- Intro" then "1. First" passed it; the converter folds a marker into the run already open at its depth whatever its kind, and the numbered items exported as bullets (or the bullet as "2." with the author "2." reading "3."); fix: the walk keeps the kind per open depth and a marker of the other kind refuses the chunk; `routes.py`
  - repro: export " - Intro" / " 1. First" / " 2. Second"; three bullets
  - test-tags: unit
  - log: 2026-08-27T19:02:20Z @kj added
  - log: 2026-08-27T19:09:29Z @kj closed: fixed: found by round 13 (architect, ux-designer, bug-hunter); regression test mutation-proved; 354 pytest green, galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
- [x] `DEF-MARK-92` **an invisible border in rgba or hex-alpha notation draws a grey box** - MINOR; only the keyword "transparent" wrote a side off; rgba(0,0,0,0), #0000 and #00000000 resolve to no colour, did not equal the keyword, and counted as a drawn side with the fallback grey bar; #RRGGBBAA was rejected outright so a translucent red bar turned grey; fix: hex alpha is composited as rgba alpha is, and a colour notation resolving to nothing writes the side off; `routes.py`
  - repro: export a div styled "border: 1px solid rgba(0,0,0,0)" to DOCX; a grey-barred table appears
  - test-tags: unit
  - log: 2026-08-27T19:02:21Z @kj added
  - log: 2026-08-27T19:09:29Z @kj closed: fixed: found by round 13 (architect, ux-designer, bug-hunter); regression test mutation-proved; 354 pytest green, galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
- [x] `DEF-MARK-93` **the fill latched on the first background colour** - MINOR; "background: red; background: none" kept the red - the fill took the first colour any background declaration named and was never cleared, where the cascade last value wins; fix: background and background-color assign the fill, last wins, a value naming no colour clears it; the other background-\* longhands never carry the colour; `routes.py`
  - repro: export a div styled "background: red; background: none" to DOCX; a solid red box
  - test-tags: unit
  - log: 2026-08-27T19:02:21Z @kj added
  - log: 2026-08-27T19:09:29Z @kj closed: fixed: found by round 13 (architect, ux-designer, bug-hunter); regression test mutation-proved; 354 pytest green, galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
- [x] `DEF-MARK-94` **a semicolon inside a url() tore the declaration** - MINOR; style attributes were split on every ";", so url(tan;x.png) left "url(tan" whose last word is a named colour and a tan callout was drawn for a background image the export cannot show; fix: one \_css_declarations splitter that skips ";" inside quotes and parentheses, used at all five split sites; `routes.py`
  - repro: export a div styled "background: url(tan;x.png)" to DOCX; a tan box
  - test-tags: unit
  - log: 2026-08-27T19:02:21Z @kj added
  - log: 2026-08-27T19:09:29Z @kj closed: fixed: found by round 13 (architect, ux-designer, bug-hunter); regression test mutation-proved; 354 pytest green, galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
- [x] `DEF-MARK-95` **depth walk keyed on the column, the converter buckets by tab_length** - MINOR; the list processors take up to tab_length-1 extra spaces as the same level, so a bullet one column deeper than the numbered items around it is their sibling and was merged into their run - 1., 2., 3. where the author wrote 1., a bullet, 2. - and the DEF-MARK-91 walk, keyed on the exact column, did not see it; fix: depth is the converter bucket, (column - outermost) // 2; `routes.py`
  - evidence: test_depth_is_the_converter_bucket_not_the_column; regression test in test_export_fidelity.py, mutation-proved; 359 pytest green, galata 5/5, 8,949-file differential 15 changed / 15 better / 0 worse
  - repro: export " 1. a" / " - b" / " 2. c"; the bullet reads 2. and the author 2. reads 3.
  - test-tags: unit
  - log: 2026-08-27T19:53:14Z @kj added
  - log: 2026-08-27T19:57:31Z @kj closed: fixed: found by round 14 (architect and bug-hunter converged)
- [x] `DEF-MARK-96` **a marker the converter cannot nest is kept as item text** - MINOR; bullets nested four columns under a two-column parent, or an ordered list aligned three columns per level, render as one list with the deeper markers literal inside the parent item - one changed block, code to list, nothing block-level, code text equal - and the author level is gone where the control showed it; fix: the rescued list must hold exactly as many items as the chunk has marker lines; `routes.py`
  - evidence: test_every_marker_line_must_arrive_as_an_item; regression test in test_export_fidelity.py, mutation-proved; 359 pytest green, galata 5/5, 8,949-file differential 15 changed / 15 better / 0 worse
  - repro: export " - a" / " - a1" / " - a2" / " - b"; the PDF shows "- a1" as text at the bullet level
  - test-tags: unit
  - log: 2026-08-27T19:53:14Z @kj added
  - log: 2026-08-27T19:57:31Z @kj closed: fixed: found by round 14 (ux-designer)
- [x] `DEF-MARK-97` **hsl() written off as an invisible border** - MINOR; the DEF-MARK-92 write-off was keyed on notation, so a functional colour the parser did not read - hsl() - was treated as alpha zero and a document boxed with it exported boxed in HTML and unboxed in Word and PDF; fix: hsl()/hsla() are read, \_normalize_css_color answers None for a value it cannot read and empty for one that is invisible, and only the invisible one writes the side off; `routes.py`
  - evidence: test_an_unreadable_colour_keeps_its_box_and_an_invisible_one_does_not; regression test in test_export_fidelity.py, mutation-proved; 359 pytest green, galata 5/5, 8,949-file differential 15 changed / 15 better / 0 worse
  - repro: export a div styled "border: 2px solid hsl(120, 100%, 25%)" to DOCX; no box
  - test-tags: unit
  - log: 2026-08-27T19:53:14Z @kj added
  - log: 2026-08-27T19:57:00Z @kj also fixed: the border shorthand parser split a function call on its inner spaces, handing "100%" of hsl(0 100% 50%) to the width; a call is one token now
  - log: 2026-08-27T19:57:31Z @kj closed: fixed: found by round 14 (bug-hunter)
- [x] `DEF-MARK-98` **run shading latched the first background colour** - MINOR; the pill reader kept the last colour any background declaration named and never cleared it, while the box reader (DEF-MARK-93) reads last-wins - one declaration block read two ways, a red band behind the run where the author last word was none; pre-existing at HEAD; fix: the pill reader assigns the fill from every background/background-color declaration, last wins; `routes.py`
  - evidence: test_the_run_shading_reads_the_cascade_as_the_box_does; regression test in test_export_fidelity.py, mutation-proved; 359 pytest green, galata 5/5, 8,949-file differential 15 changed / 15 better / 0 worse
  - repro: export a span styled "background: red; background: none" to DOCX; the run is shaded FF0000
  - test-tags: unit
  - log: 2026-08-27T19:53:14Z @kj added
  - log: 2026-08-27T19:57:31Z @kj closed: fixed: found by round 14 (bug-hunter)
- [x] `DEF-MARK-99` **PDF ordered numbering continued across headings** - MINOR; process_paragraph numbered lists from one document-wide counter that only a normal paragraph or a quote reset, so a procedure under a heading read 3., 4. where the author wrote 1., 2.; pre-existing for column-0 lists, widened by every rescued list whose control showed the literal digits; fix: the counters reset wherever the list level resets - a heading, a quote; `routes.py`. Word numbers List Number paragraphs from one numId with no restart per list - the same harm in DOCX, pre-existing, open: see DEF-MARK-100
  - evidence: test_pdf_ordered_numbering_restarts_after_a_heading; regression test in test_export_fidelity.py, mutation-proved; 359 pytest green, galata 5/5, 8,949-file differential 15 changed / 15 better / 0 worse
  - repro: export "1. alpha" / "2. beta" / "## Section two" / " 1. one" / " 2. two" to PDF; the second list reads 3., 4.
  - test-tags: integration
  - log: 2026-08-27T19:53:14Z @kj added
  - log: 2026-08-27T19:57:32Z @kj closed: fixed: found by round 14 (ux-designer)
- [x] `DEF-MARK-100` **Word numbers every List Number paragraph from one sequence** - MINOR; htmldocx gives every ordered item the List Number style whose numId carries no restart, so Word numbers successive ordered lists 1-2, 3-4, 5-6 whatever the author wrote; pre-existing at HEAD for every column-0 ordered list; a rescued list under a heading shows the same continued count where the control showed the literal digits; cause: no per-list w:num with a startOverride; `routes.py`
  - evidence: test_docx_each_ordered_list_has_its_own_numbering_instance - two lists on distinct w:num with startOverride 1, a step broken by a table on one; mutation-proved (mark removed fails it)
  - repro: export "1. alpha" / "2. beta" / "## S" / "1. one" / "2. two" to DOCX; Word shows 3., 4.
  - test-tags: manual
  - log: 2026-08-27T19:53:14Z @kj added
  - log: 2026-08-27T22:35:03Z @kj closed
  - log: 2026-08-27T22:46:10Z @kj shipped in commit 9480951 on main
- [x] `DEF-MARK-101` **PDF ordered numbering counted on past every separator but a heading and a quote** - MINOR; the DEF-MARK-99 reset named a heading and a quote; a rule, a table, a fence, an image, an alert and a boxed div still let the next procedure continue the count, and a boxed div was a column-0 regression - a paragraph that reset the count became a callout table that did not; a bullet parent also left the ordered run beneath it counting on from the previous parent; fix: one reset_numbering() at every body element that is not a list paragraph, and a bullet clears the counters below its level; `routes.py`
  - evidence: test_pdf_ordered_numbering_restarts_at_every_separator, mutation-proved; galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
  - repro: export "1. alpha" / "2. beta" / "---" / " 1. one" / " 2. two" to PDF; the second list reads 3., 4.
  - test-tags: integration
  - log: 2026-08-27T21:45:21Z @kj added
  - log: 2026-08-27T21:47:18Z @kj closed: fixed: found by round 15 (all three lenses converged)
- [x] `DEF-MARK-102` **a line carrying two markers defeated the item count** - MINOR; "- 1. Intro" is two items to the converter and one marker line to the pass, so the DEF-MARK-96 count matched while a level the converter could not nest below it arrived as literal text; fix: a chunk whose marker line opens with a second marker is refused; `routes.py`
  - evidence: test_a_line_carrying_two_markers_refuses_the_rescue, mutation-proved; galata 5/5, 8,950-file differential 15 changed / 15 better / 0 worse
  - repro: export " - 1. Intro" / " - sub"; the PDF prints "- sub" as text
  - test-tags: unit
  - log: 2026-08-27T21:45:21Z @kj added
  - log: 2026-08-27T21:47:18Z @kj closed: fixed: found by round 15 (bug-hunter)
- [x] `DEF-MARK-103` **PDF ordered numbering restarted at any block inside a step** - MAJOR; the DEF-MARK-99 and DEF-MARK-101 resets guessed a list's end from the Word paragraphs, and a table, a rule, a sample, a paragraph or a quote inside a step reads exactly like one standing between two lists - list paragraph, something, list paragraph - so '1. Run:' + fence + '2. Then' numbered 1., 1. in the PDF; regression against HEAD for a table, a rule and a sample, pre-existing for a paragraph and a quote; fix: the boundary is carried, not guessed - restructure_html_for_docx marks the first item of every ol (U+2062), restart_docx_list_numbering gives each list its own numbering instance and removes the mark, and the PDF restarts only where the instance on the paragraph changes; every other reset is gone; routes.py
  - related: DEF-MARK-99, DEF-MARK-101 - the guesses this replaces; DEF-MARK-100 - closed by the same instance per list
  - evidence: test_pdf_ordered_numbering_continues_across_a_block_inside_a_step, mutation-proved (a reset at w:tbl fails it; the mark removed fails it with the separator test); existing separator and heading tests still pass
  - repro: export '1. Run:' / blank / ' `' / '   cmd' / '   `' / blank / '2. Then' to PDF; the second step reads 1.
  - log: 2026-08-27T22:34:47Z @kj added
  - log: 2026-08-27T22:35:03Z @kj closed
  - log: 2026-08-27T22:46:11Z @kj shipped in commit 9480951 on main
- [x] `DEF-MARK-104` **a reference definition under an indented item vanished in the rescue** - MINOR; the converter consumes a link reference definition as a definition and shows it nowhere, while the control showed the URL inside its code block; every rule-3 check passed because the block census does not see a definition; fix: a chunk holding a line of the shape [name]: is never a candidate; routes.py
  - evidence: test_a_reference_definition_in_the_chunk_is_never_a_candidate, mutation-proved
  - repro: export 'Intro.' / blank / ' - a' / ' [ref]: http://example.com/x' / blank / 'End.'; the URL is absent from every format
  - log: 2026-08-27T22:34:48Z @kj added
  - log: 2026-08-27T22:35:03Z @kj closed
  - log: 2026-08-27T22:46:11Z @kj shipped in commit 9480951 on main
- [x] `DEF-MARK-105` **PDF skipped the number of an empty ordered item** - MINOR; an item with no text is a List Number paragraph that Word numbers, but process_paragraph took the empty-text exit before reading the style, so '1. a / 2. / 3. c' printed 3. c as 2. c; pre-existing at HEAD, and round 15 had turned the skip into a restart; fix: the empty-text exit no longer applies to a numbered paragraph; routes.py
  - evidence: test_pdf_an_empty_numbered_item_keeps_its_number, mutation-proved
  - repro: export '1. a' / '2. ' / '3. c' to PDF; the third item reads 2.
  - log: 2026-08-27T22:34:48Z @kj added
  - log: 2026-08-27T22:35:03Z @kj closed
  - log: 2026-08-27T22:46:11Z @kj shipped in commit 9480951 on main
- [ ] `DEF-MARK-106` **a one-space fence lets the sample extractor cut a rescued chunk** - MINOR; highlight_code_blocks and extract_code_blocks cut samples with an unanchored backtick regex before the converter runs, so a fence indented one space - a paragraph to python-markdown, a fence to a CommonMark preview - whose body holds a blank line and then a two-space marker chunk is measured as [p, pre, p] -> [p, ul, p] and rescued, and the DOCX and PDF sample then carries the shifted bytes; pre-existing extractor, out of the indent pass's diff; open
  - repro: export ' `' / ' x' / blank / '  - a' / '  - b' / blank / ' `' to DOCX; the sample reads '- a' at column 0
  - log: 2026-08-27T22:34:48Z @kj added
- [ ] `DEF-MARK-107` **an alert inside a numbered step splits the list** - MINOR; preprocess_github_alerts lifts an indented '> [!NOTE]' to column 0 and leaves the quoted lines indented, so the converter closes the list, renders the quoted line as a code block and opens a second list; HTML export shows one list of two items; pre-existing at HEAD, with the second list now numbered from 1 in Word too since each list has its own instance; open
  - related: DEF-MARK-100 - the split's second list now restarts in Word as well
  - repro: export '1. Step' / blank / ' > [!NOTE]' / ' > noted' / blank / '2. Next' to DOCX or PDF; the second step reads 1.
  - log: 2026-08-27T22:34:48Z @kj added
- [x] `DEF-MARK-108` **a third-level list resets its parent's count in the PDF** - MAJOR; The PDF walker read every indent past the first as level 1, so a depth-2 ordered list shared the depth-1 counter and numbering-instance slot; returning to the parent looked like a change of list and restarted it at 1 where Word printed 3. Found by the reading-only architect round over commit 9480951
  - evidence: get_list_info now uses the DOCX pass's depth formula in twips; test test_pdf_a_third_level_list_does_not_reset_its_parent, mutation-proved against the binary level
  - related: DEF-MARK-103 - the carried-signal reset this partition mismatch undermined
  - repro: export '1. o0 / 1. o1 / 2. o1b / 1. o2 / 3. o1c' (two-space nesting) to PDF and read the number before o1c
  - log: 2026-08-27T22:50:49Z @kj added
  - log: 2026-08-27T22:50:49Z @kj closed
- [x] `DEF-MARK-109` **a callout's left accent lost to its neutral frame** - MAJOR; A hand-drawn callout written as a neutral frame with a coloured left edge (border: 1px solid #ddd; border-left: 4px solid #0969da) drew a grey bar in Word and PDF: \_css_callout_box kept the first drawn side in top-right-bottom-left order, so the frame colour won and an info box and a danger box came out the same. Found by the reading-only ux round over commit 9480951
  - evidence: the bar reads the left side first; test test_the_bar_takes_the_left_edge_colour_over_the_frame, mutation-proved against the old order
  - repro: export a div styled 'border: 1px solid #ddd; border-left: 4px solid #0969da' and read the bar colour in the DOCX table borders
  - log: 2026-08-27T22:58:18Z @kj added
  - log: 2026-08-27T22:58:18Z @kj closed
- [x] `DEF-MARK-110` **inline math in a two-space list stayed a code block in the PDF** - MAJOR; The PDF handler rewrites inline math to an <img> before normalize_list_indentation runs, and the rescue refused any chunk holding a tag anywhere on a line, so ' - Energy $E=mc^2$' was a list in Word and HTML and a code block holding the base64 blob in the PDF. Found by the reading-only ux round over commit 9480951
  - evidence: verbatim_re refuses a tag only at a line start; test test_an_inline_tag_in_an_item_does_not_refuse_the_rescue, mutation-proved; the raw-block test DEF-MARK-86 still passes
  - related: DEF-MARK-86 - the raw-block refusal this narrows to a line-opening tag
  - repro: export a document with ' - Energy $E=mc^2$' under a paragraph to PDF and read the block
  - log: 2026-08-27T22:58:19Z @kj added
  - log: 2026-08-27T22:58:19Z @kj closed
- [x] `DEF-MARK-111` **trailing whitespace on the last item refused the rescue** - MINOR; python-markdown stores an indented code block rstripped, so a stray space after the last two-space item made the control's code text differ from the chunk and the whole list stayed a monospace source block. Found by the reading-only bug-hunter round over commit 9480951
  - evidence: the chunk side of the comparison is rstripped too; test test_trailing_whitespace_on_the_last_item_does_not_refuse_the_rescue, mutation-proved
  - repro: normalize 'Intro.\n\n - a\n - b \n\nEnd.\n' and see it returned unchanged
  - log: 2026-08-27T23:01:22Z @kj added
  - log: 2026-08-27T23:01:22Z @kj closed
- [ ] `DEF-MARK-112` **the rescue pass renders the whole document once per candidate chunk** - MEDIUM; normalize_list_indentation measures each candidate by converting the entire document, synchronously in the handler. Measured 2026-08-28: the 87 KB SOW takes 367 ms and thirteen sibling documents 0 ms (no candidates); a synthetic 158 KB document with 100 loose sub-bullets and 20 fences takes 5.4 s at 96 ms per render. Found by the reading-only bug-hunter round; a candidate budget or a cheaper oracle are the remedies, neither taken while real documents stay under half a second
  - related: DEF-MARK-90 - the oracle design whose cost this records
  - repro: time normalize_list_indentation on a 158 KB document holding 100 '- top / (blank) / - sub' pairs
  - log: 2026-08-27T23:01:22Z @kj added

## Diagram rendering `DIAG`

- [x] `DEF-DIAG-29` **a CSS colour written as `rgb(...)` is dropped in DOCX and PDF** - MEDIUM; `_normalize_css_color` resolves hex and named colours only, so `background-color:rgb(255,255,0)` falls through to htmldocx, which writes a `w:highlight` from its fixed palette that nothing downstream reads: Word shows `lightGray`, the PDF draws no fill at all, and the HTML export shows yellow - three formats, three answers; `color:rgb(...)` renders black the same way; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the third confirming round. `rgb()` is the notation a rich-text paste produces, so it is not exotic. Not fixed here: a colour-notation parser is new code the reported defect does not ask for, and the fix belongs with `DEF-MARK-24`, which needs a marker pass of its own anyway
  - log: 2026-08-27T00:00:00Z @kj re-measured before fixing, and the register understated it: of the six forms, only `color: rgb(220, 38, 38)` renders, and by htmldocx's own parse rather than this code; `background-color: rgb(255,255,0)` is the registered `lightGray` highlight; and `color: rgb(100%, 0%, 0%)`, `color: rgba(220, 38, 38, 0.45)` and `background-color: rgba(255, 255, 0, 0.5)` each take the whole export down with `ValueError: invalid literal for int() with base 10` - HTTP 500 and no document, not a wrong colour
  - log: 2026-08-27T00:00:00Z @kj fixed: `_normalize_css_color` reads `rgb()` / `rgba()` in the comma notation and in the space notation CSS Color 4 added, numbers and percentages alike, clamping each channel the way a browser clamps it. The alpha channel is parsed and dropped - Word has no translucent run or cell and reportlab paints a callout opaque, so a translucent colour can only arrive at full strength. The background branch now scans its value for a colour instead of taking the first whitespace token, which a spaced `rgb(...)` splits into pieces that are no colour at all
  - log: 2026-08-27T00:00:00Z @kj the named table was widened from 26 names to the whole CSS list of 148, two local sources agreeing entry for entry and both a strict superset of the 26, so no existing colour changes value. Which names "show up in practice" is not knowable, and a name the table misses renders black with nothing to say it was ever read
  - log: 2026-08-27T00:00:00Z @kj tests: `TestCssColorNotation`, four regressions through the export API. Mutation-proved on three reverts - the `rgb()` branch off (3 colour tests fail on a 500, plus both box tests, which take the reported callout's fill from an `rgba()`), the background branch back to its first-token slice (the `rgba()` background dies), and `lightgray` removed from the table (it exports as `000000`)
  - log: 2026-08-27T00:00:00Z @kj corrected, from the confirming review: dropping the alpha channel was the wrong reading of it. Alpha is what makes a colour light, so at full strength `rgba(0, 0, 0, 0.05)` - the commonest soft-grey wash there is - inverted to solid `000000` and painted out the black text sitting on it: measured as a full-width black rectangle in the PDF and a `w:shd` of `000000` behind default-black runs in Word, readable before the change and unreadable after. The colour is now composited onto the white page instead (`channel x alpha + 255 x (1 - alpha)`), which is what a browser draws, and `rgba(..., 0)` resolves to no colour at all - the same answer the keyword `transparent` already got, the two spellings of one value having disagreed. The reported document's own box moves with it, `F4F4F5` to `FAFAFA`
  - log: 2026-08-27T00:00:00Z @kj hardened, from the same review: `nan`, `inf` and an overflowing exponent are values `float()` accepts and `round()` refuses, and the guard covered only the parse - so the branch written to stop a malformed colour killing the export reintroduced exactly that, HTTP 500 and no document, asymmetrically (HTML shipped one, DOCX and PDF did not). The guard now spans the conversion and catches `OverflowError` too. A second hole behind it: a colour this code cannot resolve was still handed on in the style attribute, and htmldocx reads one channel at a time with `int()` - so the export died on `invalid literal for int() with base 10: ''` instead. A value carrying the functional notation is now dropped rather than passed through, losing one colour instead of the document
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-30` **`_HTML_INLINE_TAGS` and the branch it selects are inert** - MEDIUM; `restructure_html_for_docx` chooses between wrapping an element in its emphasis tag and wrapping the element's contents, and a differential over 59 documents - including 17 written to separate the two branches - found identical paragraph structure, identical per-run properties and identical PDF spans either way; the whitelist is 11 lines a maintainer must extend whenever a new inline tag appears, guarding a distinction with no observed effect; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the third confirming round, which proposed deleting it. Declined for now rather than declined outright: the branch exists because the first round found a CRITICAL in exactly this code - `el.wrap()` between `<tr>` and `<td>` deleted the cell - and removing the guard that closed it is not a change to make in the same pass that is still finding regressions. Worth doing on its own, with the differential re-run
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-31` **a PDF highlight has a hairline notch at each internal run boundary** - MEDIUM; reportlab derives a fragment's background from its own advance and omits a trailing space's, so `<mark>**BOLD** [L](...) plain</mark>` paints four bands with a ~3.8pt unfilled gap where the space between two inline children sits; Word shades the run and draws one continuous band, so the two formats disagree on a highlight the reader compares side by side; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the third confirming round, found independently by two lenses. The DOCX half was fixed with it: the whitespace text node inside a background now carries the fill (it previously carried none), so Word is continuous. The PDF half needs adjacent same-fill runs coalesced into one `<font backColor>` fragment before the string reaches reportlab, which is a change to how the paragraph markup is assembled - not one to make in the same pass that is still finding regressions
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-32` **the italic slant is dropped for a whole run when one character is uncoverable** - MEDIUM; the PDF italic slot is filled from Liberation (DejaVu ships no oblique on most Linux boxes) and Liberation has no glyph for `U+2605 U+2606 U+2713 U+2714 U+2717 U+2718 U+2610 U+2612`, so `pdf_face_covers` renders such a run upright rather than let reportlab paint a blank advance; the granularity is the run because the PDF path does no run splitting, so one star in a blockquote costs the whole quote its slant; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered while fixing the star deletion it replaced. The trade is deliberate - a visible character beats a slanted gap - but the narrower fix is to segment `run.text` at the uncoverable characters, the way `style_docx_symbol_runs` already does on the DOCX side, and emit the slant only around the covered stretches
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-33` **an alert box's `w:tblBorders` children are out of schema order** - CRITICAL; `w:left` is emitted after `w:right` and `w:insideV`, which `ISO-IEC29500-4_2016/wml.xsd` rejects; Word evidently tolerates it, since alert boxes ship and have never been reported broken; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the third confirming round, which validated 88 documents against the schema and found this as the only invalid construct. Identical at HEAD - `> [!WARNING]` alone reproduces it on both trees - so not from this change
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-34` **a `<div>` inside a list item, heading or table cell cut the block in two** - MEDIUM; the inline-div rule renamed a childless `<div>` to `<p>` whenever no `<p>` ancestor existed, but a `<li>`, `<td>`, `<th>` and every `<hN>` already own a run of text, so the nested block made htmldocx end the item, cell or heading early and the rest of the line became a paragraph of its own - measured: `# H <div>MID</div> tail` exported as a Heading 1 holding `H` plus two Normal paragraphs; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj found by the fourth (correctness-only) round; a regression from the `DEF-MARK-23` inline-div fix, whose guard named only `<p>` because markdown's own inline wrapping was the case in hand. The guard now tests the full `_HTML_TEXT_HOLDERS` set and the div becomes a `<span>` there, keeping its style attribute for the colour pass. Regression test `test_a_div_inside_a_text_holder_keeps_the_block_whole`, mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-35` **`pdf_face_covers` answered blind for a core Type1 face** - MEDIUM; on a box with no oblique TTF at all the italic slot falls back to `Helvetica-Oblique`, whose reportlab face has no `charToGlyph`, and the guard's except-branch answered True for any name it could not introspect - so exactly on the machine the guard exists for it went inert, and a U+2605 in an italic run came out as a symbol-encoding dingbat; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj found by the fourth round. The probe now distinguishes a name nothing registered (keep the styling) from a registered Type1 face (WinAnsi-only: covered means every codepoint below 256). Verified by matrix: `Helvetica-Oblique` + `U+2605 U+2713 U+2612` False, + plain ASCII True, `UnicodeSansItalic` + U+2605 False, + `zażółć gęślą` True, unregistered name True
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-36` **the heading upright fallback could swap into a face no better** - MAJOR; the `DEF-DIAG-32` trade for headings replaced an italic heading face with its upright sibling unconditionally, so when the upright face also lacked the glyph the heading lost its slant and still lost the character; the swap now fires only when the fallback actually covers the text; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj found by the fourth round; regression test `test_a_minor_heading_keeps_its_symbols` extended by the conditional, mutation-proved with the rest of the batch
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-37` **an inline-math marker leaks into a table cell's text** - MEDIUM; a `$...$` inside a table cell leaves its U+2063-fenced OMML marker visible in the exported cell because the cell path rebuilds text without running the marker merge; pre-existing at HEAD, not widened by this change; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the fourth round's bug hunter, which flagged it out of scope for the `DEF-MARK-22`/`DEF-MARK-23` fix. The in-paragraph merge (`merge_inline_math_omml`) walks document paragraphs; extending it to table cells is its own change with its own surface
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-38` **a literal twin of a wrapper tag un-formatted the text after it** - MAJOR; the CSS-to-tag pass wrapped `<p style="font-weight:bold">hot <b>stuff</b> more</p>` in a second `<b>`, and htmldocx keys its open tags by name and pops on the first close - so the inner `</b>` ended the outer wrapper and ` more` arrived regular in DOCX and PDF while every browser bolds it; the ancestor guard checked `find_parent` only, blind to a descendant twin; same route for `<i>` in `font-style:italic` and, via the alias pass, `<ins>`/`<del>` in their text-decoration twins; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj found by the fifth (confirming) round's architect lens; new in this change - the wrapper mechanism itself is new. Fixed by unwrapping same-name descendants before wrapping: bold inside bold is bold, so the wrapper subsumes the twin and the emitted HTML shrinks. Regression test `test_a_literal_twin_does_not_unbold_the_tail`, mutation-proved
  - log: 2026-08-26T00:00:00Z @kj hardened by the sixth round, which found the unwrap took too much: it destroyed the twin's OWN attributes - `<b style="color:red">` lost its colour, `<b id="t1">` its anchor - and the inline path's re-wrap made the element its own next twin, unwrapping its kept styling; a twin inside a nested table also lost its weight to a wrapper that cannot reach that cell's runs. The subsume now takes only the NAME - a rename to `<span>` keeps style, id and place in the style snapshot - and skips twins across a nested-table boundary, where htmldocx scopes each cell so no early close crosses. Regression test `test_a_twin_keeps_its_colour_anchor_and_cell`, both halves mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-39` **declaration accumulation ignored the CSS cascade** - MEDIUM; wrapper tags were concatenated per declaration, so `text-decoration:underline;text-decoration:none` underlined in Word what a browser draws plain, `font-weight:bold;font-weight:normal` stayed bold, and a duplicated `bold` double-wrapped - recreating `DEF-DIAG-38`'s twin from whole cloth; self-overriding pairs are what a rich-text paste produces, the same provenance as `DEF-DIAG-29`; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj found by the fifth round's architect lens. Fixed by folding declarations into a per-property dict - last value wins, as the cascade says - and mapping to wrapper tags once after the loop. Regression test `test_the_last_declaration_wins`, mutation-proved against a `setdefault` (first-wins) mutant
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-40` **`<center>` welds its text onto the previous paragraph and drops the centring** - MEDIUM; the block-vs-inline rescue added for `<div>` names `div` alone, so `<center>Centred</center>` between two paragraphs joins the paragraph already open with no space and no alignment, in DOCX and PDF both; the exact failure class the `<div>` pass fixed, on the unhandled sibling tag; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the fifth round's ux lens; pre-existing - the diff neither introduced nor touched it. The remedy is an alias of `center` to `div` with `text-align:center` ahead of the div pass
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [ ] `DEF-DIAG-41` **bold on a `<tr>` is dropped in DOCX and PDF while the HTML export shows it** - MEDIUM; `<tr style="font-weight:bold">` clears its wrappers because Word has no run inside a row and either wrapper placement loses the cells in htmldocx's direct-child walk; the in-code comment records the trade - losing emphasis beats losing cells; `routes.py`
  - log: 2026-08-26T00:00:00Z @kj registered from the fifth round's ux lens so the trade has a number; the closing move is distributing the wrapper per `<td>` instead of clearing it
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-4` **mermaid diagrams rasterized with a view window far larger than the shape** - MEDIUM; a mermaid SVG scaled to the correct page width but sat inside a canvas much larger than the visible diagram, drowning it in whitespace; cause: `PlaywrightSvgRenderer.render` trusted the SVG's declared `viewBox`, which for many mermaid outputs is much larger than the real content bounding box; fix: measure the rendered content bbox with `getBBox()` in the browser and tighten the viewBox (small padding) before screenshotting; `routes.py`
  - log: 2026-07-22T00:00:00Z @kj reported: "mermaid diagrams ... rendered without proper view window - view window waaaaay outside the visible shape"; reproduced - a 100x40 shape in an 800x800 viewBox rasterized to a 600x600 PNG with content filling 13% width / 5% height (99% empty)
  - log: 2026-07-22T00:00:00Z @kj fixed: getBBox tight-crop in `render`; the same 100x40-in-800x800 shape now fills ~96% width / ~91% height; mutation-proved
  - log: 2026-07-23T00:00:00Z @kj hardened: added regression tests so the edge cases trip early without re-rendering a real diagram - `test_well_framed_diagram_is_not_cropped` (a diagram that already frames its content is left uncropped, since getBBox omits stroke/markers), `test_nonzero_viewbox_origin_is_preserved` (a non-zero viewBox origin is honoured, not zeroed); both mutation-proved
  - log: 2026-07-23T00:00:00Z @kj accepted trade-offs (adversarial rounds, not fixed): the crop uses `getBBox` (geometry only), so on a diagram that trips the <0.8-fill crop a marker or thick stroke exactly at the outer boundary could be clipped - not seen in practice since a diagram's extremity is a node whose geometry is measured, and the 4% pad adds headroom; and an extreme narrow-tall crop inside a much-wider viewBox could drive the raster past Chromium's ~16384px limit - unreachable for real mermaid, which frames tightly (fill ~1.0) so the crop never fires. Both left as-is pending a concrete failing diagram
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-7` **regression: mermaid diagrams rasterize mostly as whitespace** - MEDIUM; reported "inserted as mini svg-s and most of the svg is whitespace as if view window is miscalibrated"; plain `.svg` images were unaffected. Reproduced on `00-inception-poc-owt.md` by rendering its 10 real mermaid blocks with mermaid 11.12.1 and rasterizing through `PlaywrightSvgRenderer`: worst diagram filled **43.3% width / 26.7% height**. Two independent causes in the same path: (a) mermaid stamps an inline `style="max-width: <natural>px"` on the SVG root, which caps the element so the renderer's `width` assignment is ignored and the diagram paints at natural size inside the target canvas - fixed by clearing `maxWidth`/`maxHeight` before sizing; (b) the crop pad was `0.04 * max(w, h)`, one value from the larger axis applied to both, so a wide short flowchart (844x70) got a 34pt vertical pad that nearly doubled its height - fixed by taking the single pad from the SMALLER extent instead. After both, the same 10 diagrams fill **97.1-98.9% width / 81.4-91.4% height**.; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj fixed: deterministic regression test `test_docx_mermaid_image_is_not_mostly_whitespace` pulls the image back out of the .docx, trims the blank margin and asserts the ink spans >=85% of the image width; mutation-proved (39% without the fix)
  - log: 2026-07-23T00:00:00Z @kj hardened: the first pad fix padded each axis by its own extent, which a UX round showed trades one asymmetry for another - a wide flowchart came back with side gutters five times its top ones, and a tall column had its aspect ratio stretched further. One pad from the smaller extent gives an equal printed margin all round (the crop is scaled uniformly) and measures better on the real document: 97.1-98.9% width against 92.8-98.1%
  - log: 2026-07-23T00:00:00Z @kj hardened: an extreme aspect ratio (a long single-column `graph TD`) rasterizes past Chromium's ~16384px viewport and texture caps at the configured export width; the screenshot throws, the renderer falls back to the original SVG data URI, and htmldocx cannot embed SVG - so the diagram vanished from the export with nothing surfaced. The raster is now scaled down to `MAX_RASTER_PX` with the aspect intact; regression test `test_extreme_aspect_diagram_stays_within_the_raster_limit`, mutation-proved (400x8400 unclamped)
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-DIAG-16` **mermaid diagrams are not rendered when the export is driven through the API** - MAJOR; mermaid is a browser library, so the frontend renders each diagram in the page and posts it as `mermaidDiagrams`; `replace_mermaid_with_images` is a pure lookup into that payload, so a caller hitting the REST endpoints directly - a script, curl, a scheduled job - sent none and every diagram exported as its own source code; fix: a server-side pass renders whatever the frontend left, using a vendored mermaid bundle inside the same headless Chromium that rasterizes SVGs; `routes.py`, `package.json`, `pyproject.toml`
  - log: 2026-07-23T00:00:00Z @kj reported: "when exporting docx or pdf from md that has mermaid diagrams - via API, mermaid isn't rendered"; example `00-inception-poc-owt.md`. Reproduced through `jp_fetch` with no `mermaidDiagrams` in the body: 10 mermaid fences in, **0 images** in the DOCX and 10 literal `flowchart` sources in `word/document.xml`
  - log: 2026-07-23T00:00:00Z @kj fixed: `PlaywrightSvgRenderer.render_mermaid` loads `vendor/mermaid.min.js` in one page and renders every source through it with the options this extension's own frontend renders with (`securityLevel: 'loose'`, plus the `maxTextSize`/`maxEdges` ceilings it inherits from `@jupyterlab/mermaid`), theme following the export theme; `render_mermaid_server_side` substitutes each result as an inline image, so from there it takes exactly the path a frontend-rendered diagram takes. The same document now exports 10 images in DOCX (97.1-98.9% ink width), 10 in PDF and 10 inline SVGs in HTML, with no source text left in any of them. Six regression tests, all mutation-proved
  - log: 2026-07-23T00:00:00Z @kj fixed (contract): an export never fails over a diagram - no Chromium, no bundle, a syntax error, a layout past the 30s timeout and a failed rasterization each keep that diagram's source and return 200. What went wrong is reported instead, in an `X-Export-Warnings` response header carrying a JSON array of `{code, count, diagrams, message}`: the body is a binary document, so a header is the only channel an API caller has, and the message is the whole remedy (`chromium-unavailable` names the install command). Grouped by cause, indices pointing at the diagrams that failed, absent when everything rendered. The earlier shape of this fix raised a typed `CHROMIUM_UNAVAILABLE` 503 instead, which would have made HTML export - which never needed Chromium - start failing on servers where it works today
  - log: 2026-07-23T00:00:00Z @kj fixed (cost): the diagram is rasterized inside the render session rather than left as an SVG for `extract_data_uri_images` to convert, so an export starts Chromium once instead of twice (measured ~300ms per launch) and DOCX/PDF receive a PNG data URI, HTML an SVG - byte-for-byte what the frontend posts for each
  - log: 2026-07-23T00:00:00Z @kj accepted trade-offs: the pass runs AFTER the frontend substitution, so the browser's own render still wins where there is one - it carries the Lab theme and fonts, and re-rendering it would cost a browser launch for nothing. A document with no mermaid never starts a browser, so an export that works on a Chromium-less server keeps working
  - log: 2026-07-23T00:00:00Z @kj known limitation: `@mermaid-js/layout-elk` is not vendored, so a diagram declaring `layout: elk` renders in the browser (JupyterLab registers that layout) but keeps its source through the API
  - log: 2026-07-23T00:00:00Z @kj known limitation: a diagram that KEEPS its source (any degradation - no Chromium, a syntax error) inside a `> [!NOTE]` alert is then folded onto one line by `preprocess_github_alerts` and renders garbled. Narrow (a diagram nested in an alert AND failing to render) and only on an already-degraded path where every diagram is a code block and the user is warned; a diagram that DOES render is replaced by an image before the alert pass and survives intact. Making the alert pass fence-aware is deferred with the fence-mechanism unification (DEF-DIAG-19)
  - log: 2026-07-23T00:00:00Z @kj known limitation: a ` ```text `-quoted mermaid example nested inside a list item (a fence quoting a fence, inside a container) is the one shape the scanner still counts differently from marked - measured at 0 divergences across 18,000 realistic documents once this construct is excluded. It combines two rare things and appears in no real document; recorded rather than chased
  - log: 2026-07-23T00:00:00Z @kj hardened (third round): the fence-awareness added in the second round anchored indentation at column 0, so a diagram nested in a list (four or more spaces in) or behind a blockquote marker stopped being counted - JupyterLab renders both, so the browser's index 0 would have been pasted onto the server's first _unindented_ fence and every later picture shifted with it. The prefix is now `[ \t>]*` on both sides, and a quoted block's `>` markers are stripped before mermaid sees the source, which markdown does and the old regex never did. `~~~mermaid` was also newly accepted as a diagram, but `replace_math_with_images`, `preprocess_github_alerts` and the highlighting passes protect ``` fences only - a tilde-fenced diagram would have had its labels rewritten before mermaid saw them - so a tilde fence quoted but never declared a diagram. **Reversed in the sixth round below**: that reasoning weighed a residual risk to a leftover block against a certainty, and measurement against marked showed the certainty is a misassignment
  - log: 2026-07-23T00:00:00Z @kj hardened (third round, request handling): `mathPixelWidth` carried the same null-default bug as `svgPixelWidth` and was measured shipping `$x^2$` into the PDF as literal text; both now share one `_pixel_width` coercion, which also catches `OverflowError` - `Infinity` survives `json.loads` and `int(float("1e400"))` is not a `ValueError`. The frontend's fence scanner matched nothing at all in a CRLF document (JS `.` excludes `\r`). A dead browser session raised and discarded every diagram already drawn, and the rasterize call was the one step in the loop outside any timeout. The header-truncation branch added in the second round was deleted: it defended a 4000-character Tornado limit that does not exist in the installed 6.5.7, and the real worst case is 3.1KB, so `MAX_REPORTED_DIAGRAMS` is the only bound needed
  - log: 2026-07-23T00:00:00Z @kj hardened (adversarial round, architect + bug hunter): the round confirmed a **security defect** - mermaid keeps HTML labels, so a label of `A["<img src='http://169.254.169.254/...'>"]` survives into a `<foreignObject>` and the export made the SERVER fetch that URL; measured 3 outbound requests from one diagram, an SSRF primitive for any shared markdown on a multi-user hub. Both the render and the rasterize context now abort every request; only server-generated SVGs are blocked, so a user's own `.svg` keeps the browser behaviour it had. It also found `pip install .` broken on any pre-built tree (`ensured-targets` gained the bundle, `skip-if-exists` did not, so the build that creates it was skipped and then demanded), a `ctx.close()` throw discarding every diagram that had rendered, an unguarded `re.sub` callback that could still 500 the export, warning indices numbered over the leftover fences rather than the document's diagrams, no total time budget behind the per-diagram one, and the diagrams behind a timeout accused of timing out themselves
  - log: 2026-07-23T00:00:00Z @kj hardened (fifth round, counting measured against marked instead of argued): every previous round reasoned about which fences are diagrams from CommonMark and from reading the two scanners. The authority is neither - JupyterLab renders the preview with **marked**, and the DOM capture posts one diagram per fence marked turned into one, so marked decides what the server must count. Running the scanner against the installed marked over 6000 generated documents found four divergence classes, two of which no review round had named: ` ```MERMAID ` was counted here but not by marked (`block.languages.includes(lang)` is case-sensitive), so a case-varied code sample became a picture of itself; `~~~mermaid` was counted by marked but not here; a fence closed by its container rather than by a closing fence (end of blockquote, end of file) was dropped here and rendered by marked; and a quoted or deeply indented ` ``` ` closed a fence it did not belong to, truncating the source. Each one shifts every later picture by one. All four fixed, plus the body dedent marked applies (`indentCodeCompensation`) that made a list-indented diagram reach mermaid with the item's indentation on every line. Well-formed documents agreed with marked on sources 100% and on counts 99.7% after this round; the sixth round below closed the remaining 0.3%. `FENCE_SHAPE_COUNTS` is no longer a reading of the spec - `test_marked_agrees_with_the_server_on_every_shape` re-derives every count from the installed marked, and the two scanners agree over 12,000 fuzzed documents
  - log: 2026-07-23T00:00:00Z @kj hardened (sixth round, the marked residue was a silent misplacement): the bug-hunter and architect lenses independently reproduced the "0.3% count residue" the fifth round had waved off as marked's own corner - and it is the exact wrong-picture-on-the-wrong-diagram this feature exists to prevent. Two directions: a fence four spaces past a list item's content column was counted here but is indented code to marked (over-count), and a list item inside a blockquote, or a tab after `>`, was mis-measured and its diagram dropped (under-count). The scanner tracked only a boolean `in_list`; it now measures every fence in the coordinate marked re-lexes it in - blockquote markers stripped (a tab after `>` carries one column of the marker), tabs expanded, and the fence's indent compared against the open list item's content column via `_quote_stripped` and `_list_content_col`, mirrored in `src/index.ts`. The container-close check also moved ahead of the fence branch, so a ` ```python ` right after a quoted ` ```mermaid ` closes the quote instead of running on and eating that next fence (its span had covered the python opener). Result: count-vs-marked is 0 across 18,000 realistic documents, the two scanners agree across 12,000, and all source differences are leading whitespace mermaid ignores. The one shape still counted differently is recorded as a known limitation above
  - log: 2026-07-23T00:00:00Z @kj fixed (sixth round, a syntax error is not a dead session): `render_mermaid` decided the browser had died by matching the exception TEXT against phrases like "Connection closed" - but mermaid echoes the offending source line, so a node label reading `Connection closed` or `Target page` beside a real syntax error was read as the session dying and dropped every later diagram. Liveness is now read from `page.is_closed()` / `browser.is_connected()`, not prose; `_DEAD_SESSION_RE` is gone. A budget-exhaustion timeout is also no longer reported as the healthy diagram's `render-timeout` - it carries the true `budget-exhausted`. Regression tests `test_a_syntax_error_echoing_a_dead_session_phrase_does_not_abort` and `test_a_broken_diagram_keeps_the_healthy_ones_around_it`, the first mutation-proved by restoring prose-matching (the third diagram drops)
  - log: 2026-07-23T00:00:00Z @kj hardened (seventh round, byte-equivalent info trim): the confirming round returned CLEAN from both lenses; the one finding with teeth was that Python `str.strip()` and JS `.trim()` disagree on a few code points, so ` ```mermaid<BOM> ` (a U+FEFF after the info string) counted as a diagram in the browser - marked trims it with JS `.trim()` - but as source here, a one-off count shift. The info trim and the list-reset blankness check now use `str.strip(_JS_TRIM)` with the exact ECMAScript WhiteSpace + LineTerminator set, verified byte-equivalent to `.trim()` and to marked over 5,000 BOM/whitespace-injected documents. `bom_info` added to the parity table. The bug hunter's only note was a narrow-window `budget-exhausted`-vs-`render-timeout` label that keeps the source either way - left as-is
  - log: 2026-07-23T00:00:00Z @kj fixed (sixth round, pipeline order): a diagram inside a `> [!NOTE]` exported as the picture belonging to the NEXT diagram. `preprocess_github_alerts` folds an alert body onto one line, which erases a fence inside it completely, and it ran BEFORE the mermaid passes - so the browser counted two diagrams and the server found one. Both mermaid passes now run on the document as read, before any pass that rewrites the source; the alert's diagram survives as an image inside the note, and the ordering rule is stated on `render_mermaid_server_side` where the next pass to be inserted will be read. Two document-wide degradations (`bundle-missing`, `chromium-unavailable`) also gained a `self.log.warning`: they were reported only through `X-Export-Warnings`, so a mis-packaged wheel silently produced diagram-less exports that looked exactly like the feature not existing
  - log: 2026-07-23T00:00:00Z @kj hardened (same round, fidelity and consistency): the server was initialising mermaid with `securityLevel: 'strict'` while this extension's own frontend uses `'loose'` - under strict mermaid turns HTML labels off, so the API drew a different picture than the UI, the exact divergence the fix exists to remove. Also: an elk-layout diagram was reported as a syntax error rather than an unsupported layout; a ```mermaid nested inside a longer fence (documentation showing the syntax) was rendered into a picture of itself; the warning codes were invented in the renderer and given messages in the handler with a `.get()`fallback hiding any rename, now one module-level table; the vendored bundle is resolved through`@jupyterlab/mermaid`'s own tree so it cannot drift from the mermaid the UI uses; and the header is listed in `Access-Control-Expose-Headers` so a cross-origin caller can actually read it
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [ ] `DEF-DIAG-17` **a user-supplied SVG can still make the server fetch a URL** - MEDIUM; the SSRF closed under `DEF-DIAG-16` covers only SVGs this server generated from mermaid: `render(block_network=True)` is passed from the mermaid path alone, while `extract_data_uri_images` rasterizes a document's own inline SVG with the network open, so an `<image href="http://169.254.169.254/...">` inside it is fetched by the Jupyter server exactly as the mermaid label was; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj registered by the architect lens during the `DEF-DIAG-16` confirming round, as the wider entrance left open by the narrower fix. Not closed with it: blocking every request in the shared rasterizer would also drop web fonts and remote assets from user SVGs that render today, so the remedy is a policy decision - either accept that loss, or route the browser's requests through the `_host_is_blocked` allowlist `embed_images_as_base64` already uses, which is the same posture applied consistently. Needs its own change and its own tests
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [ ] `DEF-DIAG-18` **a UI HTML export themes its diagrams three different ways** - MAJOR; `captureMermaidDiagrams` is called with `resolvedDocxTheme` and `reThemeDom = format !== 'html'` for every format, so an HTML export keeps the live JupyterLab UI theme on the DOM path, uses the DOCX theme on the source-fallback path, and the server renders whatever `htmlTheme` says; `schema/plugin.json` scopes `docxTheme` to DOCX and PDF and `htmlTheme` to HTML, so the server is right and the frontend is the outlier - the same file exported from a dark UI gets dark diagrams from the UI and light ones from the API; `src/index.ts`
  - log: 2026-07-23T00:00:00Z @kj registered by the architect lens during the `DEF-DIAG-16` confirming round. Not fixed with it: `reThemeDom=false` for HTML was a deliberate choice in v1.6.14 (HTML's primary path was left unchanged on purpose), so changing how a UI HTML export themes its diagrams is a decision about that feature, not about this one
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [ ] `DEF-DIAG-19` **a `~~~` fence is not protected by the math, alert and highlighting passes** - MEDIUM; `replace_math_with_images`, `replace_math_with_markers` and `preprocess_github_alerts` all protect fenced regions with a backtick-only pair-matcher (` ```[\s\S]*?``` `), so a tilde-fenced block reaches them unprotected and a `$` inside it is read as math; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj registered while reversing the tilde decision under `DEF-DIAG-16`. Pre-existing and not widened by that change - a tilde block was never substituted before, so it always reached those passes; it is now narrower, because a tilde-fenced diagram that renders is replaced by an image first and only a block left un-rendered is still exposed. Not fixed here because the backtick-only matcher is one of several fence mechanisms in the module and unifying them is a refactor of working code, recorded at `docs/acc-crit-jupyterlab-export-markdown-extension.md` as deferred
  - log: 2026-07-23T00:00:00Z @kj census (architect, sixth round): six independent fence matchers live in `routes.py`, not three. The mermaid scanner `_FENCE_RE` / `iter_mermaid_blocks` is container-aware and tilde-inclusive; the task-list tracker (`preprocess_task_lists`) tracks backtick+tilde by length; and four are backtick-only - `replace_math_with_images`, `replace_math_with_markers`, `highlight_code_blocks` and `extract_code_blocks`. The last two run in the DOCX/PDF pipelines and were omitted from the earlier "three mechanisms" tally. Unifying all six behind the container-aware scanner is the deferred refactor; DEF-DIAG-19 is the marker for it
  - log: 2026-08-27T00:00:00Z @kj edited severity

## Table pagination `TABL`

- [x] `DEF-TABL-5` **empty-header layout grid detaches its blank header across a page break** - MEDIUM; a Markdown image/layout grid written with an empty header row (`|  |  |  |`, the idiom for a borderless grid) had that empty row treated as a repeating styled header: in DOCX marked `w:tblHeader` (Word repeats a blank bar and strands it), in PDF painted a blank blue bar and repeated via `repeatRows`; header ended on one page, image rows on the next; fix: detect an all-empty first row and skip header treatment - no `w:tblHeader` and `tblLook firstRow=0` in DOCX, no header background and `repeatRows=0` in PDF; `routes.py`
  - log: 2026-07-22T00:00:00Z @kj reported: "header table is detached from the rest of the table (images) - with header on one page and row on the next page"; example `01-scoping-poc-mockup-flow.md`; reproduced - the two mockup grids carry an empty header row marked repeating, the Scenario matrix (real header) is unaffected
  - log: 2026-07-22T00:00:00Z @kj fixed: `first_row_empty` guard in `style_docx_table`, `has_header` guard in `process_table`; empty grids no longer mark or paint a header, real headers keep theirs; mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-TABL-6` **PDF drops table-cell images** - MEDIUM; images inside a table cell (e.g. the mockup screenshots in `01-scoping-poc-mockup-flow.md`) did not appear in the PDF; cause: `convert_docx_to_pdf.process_table` built cells from `cell.text` only, so inline `w:drawing` images were dropped, the DOCX path renders them fine; fix: `process_table` now walks each cell's paragraphs and renders inline images as reportlab flowables scaled to the column width, via a generalized `process_image(max_width, max_height)`; `routes.py`
  - log: 2026-07-22T00:00:00Z @kj reported: found while reproducing DEF-TABL-5; the mockup grids exported in PDF with captions but no images
  - log: 2026-07-22T00:00:00Z @kj fixed: `cell_content` in `process_table` embeds cell images scaled to their column; the 9 mockup screenshots now render (3 phones per row); mutation-proved
  - log: 2026-07-23T00:00:00Z @kj hardened: two confirming adversarial rounds closed a residual `repeatRows` LayoutError. A repeated header re-emits on every continuation page, so each body row must fit in `frame_height - header_h`; an atomic image row that cannot never lays out. First pass widened the guard but keyed off the shortest body row - a second pass caught that a tall image row beside a short one slips through, so the guard now drops the repeat when `header_h + max(body_h) > frame_height` (the load-bearing fix, `splitInRow` carries the row instead), plus a `frame_height - 12` image cap for the header's 4+8pt padding; regression tests `test_pdf_tall_image_in_header_cell_does_not_crash` and `test_pdf_tall_image_body_row_under_repeating_header_does_not_crash`, both mutation-proved
  - log: 2026-07-23T00:00:00Z @kj hardened: a fourth round flagged the `max(body_h)` guard as over-broad - it dropped the repeat for a tall _text_ row too, but a text row splits fine under a repeated header (`splitInRow`), so this regressed the keep-header-on-the-page feature. Guard now keys off each body row's tallest ATOMIC (image) content, not total row height: drops the repeat only when the header fills the page or a body image cannot fit under the repeated header; regression test `test_pdf_tall_text_table_keeps_repeating_header` (header must appear on >1 page), mutation-proved
  - log: 2026-07-23T00:00:00Z @kj hardened: a fifth round found the atomic-only guard still crashed when the HEADER itself nearly fills the page - it left `avail` positive but under one text line, so a one-line body row could not fit under the repeated header (LayoutError). Guard now drops the repeat when `avail` is below one body line (`table_cell_style.leading + padding`), which also subsumes the header-taller-than-frame case; regression test `test_pdf_near_full_page_header_does_not_crash` (image sized just under the 696pt cap to land the header in that window), mutation-proved. Guard is now complete: text rows bounded by the one-line clause, image rows by the atomic-floor clause
  - log: 2026-07-23T00:00:00Z @kj fixed (fidelity): a sixth round found the PDF cell image was scaled to whatever column width its _text_ earned, so an image-only cell (empty header borderless grid - the mockup idiom) collapsed its column and rendered the image ~2pt wide (invisible). The PDF column layout now floors each column to its widest cell image (points from the `wp:extent` EMU), mirroring the DOCX `image_floors`, capped at a fair share so an image cannot starve text neighbours; the same 3-image grid now renders at ~164pt per image. Regression test `test_pdf_image_only_grid_columns_size_to_images` (min image width > 100pt), mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-TABL-8` **empty header row still rendered as a blank row** - MEDIUM; a grid written with the empty-header idiom (`|  |  |  |`) renders nothing for that row in markdown, but DOCX and PDF still emitted it as a blank bordered first row above the grid; `DEF-TABL-5` had only stripped its header styling and repeat, not the row itself; fix: drop the row outright in `style_docx_table` (DOCX) and `process_table` (PDF), keeping a flag so the body row behind it is not promoted into a header in its place; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj reported: "ms word and pdf render header row - while if empty in markdown it is never rendered"; reproduced - markdown emits an empty `<thead>` (3 blank `<th>`) and the DOCX carried 3 rows, row 0 blank with 0 images above two image rows
  - log: 2026-07-23T00:00:00Z @kj fixed: DOCX grid now exports 2 rows (blank header gone) while a real `| Name | Age |` header is untouched; the PDF needed a matching `dropped_empty_header` guard or `has_header` re-promoted the first content row into a blue header bar (caught by the existing DEF-TABL-5 PDF test); test `test_docx_empty_header_row_is_dropped`
  - log: 2026-07-23T00:00:00Z @kj hardened: an architect round found the delete was DATA LOSS on one real idiom - the predicate was text-only, and an image-on-top/caption-below grid puts its pictures in the header row, so a picture-bearing blank-text row was deleted with its images (2 rows / 2 drawings became 1 row / 0). Predicate is now "no text AND no picture" (`w:drawing` or VML `imagedata`) on both paths; regression test `test_image_only_header_row_is_kept_with_its_images`
  - log: 2026-07-23T00:00:00Z @kj hardened: same round found the delete sat behind a `tblPr is None` early-exit; a later round proved that guard unreachable (`CT_Tbl.tblPr` is `OneAndOnlyOne`, and `table.style = ` creates the element regardless), so the guard is gone and the delete simply runs first. HTML had no blank-header rule at all - `drop_empty_table_headers` gives it parity, test `test_html_empty_header_row_is_dropped`
  - log: 2026-07-23T00:00:00Z @kj hardened: the confirming round (architect, bug hunter and UX independently) found the HTML predicate was still text-only - it tests emptiness by stripping tags, which erases an `<img>` with them, so the picture-bearing row DOCX and PDF were hardened to keep was deleted in HTML. Measured: a two-image header went in with 2 `<img>` and came out with 0. The predicate now bails on embedded content, matches `<thead>` with attributes case-insensitively, unescapes entities before the blank test, and requires a surviving body row like its two siblings
  - log: 2026-07-23T00:00:00Z @kj hardened: keeping a picture-only row then exposed the opposite error - it was no longer 'empty', so Word banded it and repeated it as a header while the PDF treated the identical row as body content. Header CHROME is now a separate question from blankness (text-bearing first row, read before the delete) and both formats agree; the blank predicate itself moved to one shared `docx_row_is_blank` used by both paths and widened to `w:pict`, `w:object` and OMML as well as pictures
  - log: 2026-07-23T00:00:00Z @kj hardened: the width estimate widens row 0 by 8% for a header's bold face, but after the delete row 0 is body content - the factor stole width from the column beside it. `fit_docx_table_to_page` now applies it only when `tblLook firstRow` is still set, and the PDF's `string_width` (which selects the bold face the same way) only when `has_header`; regression tests `test_headerless_grid_columns_are_not_skewed_by_bold_widening` and `test_pdf_headerless_grid_columns_are_not_skewed`, mutation-proved at 6.6% skew in DOCX and a 274.6pt-of-500.8pt split in PDF
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-TABL-9` **a table row that fits a page is torn at the page break** - MEDIUM; a row holding a caption and its image rendered the caption at the bottom of one page and the image on the next, though the whole row would fit the following page; cause: `splitInRow=1` (added in v1.6.16 to stop `LayoutError` on genuinely oversized rows) is a per-table flag, so it also split any row that merely ran past the space left on the current page; fix: measure row heights after `wrap` and enable `splitInRow` only when some row exceeds the frame, otherwise leave it 0 so a fitting row moves to the next page whole; `routes.py`
  - log: 2026-07-23T00:00:00Z @kj reported: "when full row fits onto one page - it must be rendered as a whole, not broken down"; example `01-scoping-poc-mockup-flow.md`
  - log: 2026-07-23T00:00:00Z @kj fixed: scenario matrix - a row <= page that fits the remaining space renders in place under either setting; a row <= page that does not fit must move whole (`splitInRow=0`) and was being torn (`splitInRow=1`); a row > page must split (`splitInRow=1`) or reportlab raises `LayoutError`. Since the flag is per-table the only correct value is conditional on the measured maximum row height. Test `test_pdf_row_that_fits_a_page_is_not_split` asserts the caption and its image share a page; mutation-proved (caption page 0, image page 1 when forced always-split)
  - log: 2026-07-23T00:00:00Z @kj hardened: an architect round found the same always-split flag on the alert/blockquote callout table in `make_callout`, tearing a callout that would fit the next page whole; it now measures after `wrap` and sets `splitInRow` conditionally, same rule as the content table. The content-table guard is also header-aware - under a repeating header a body row only has `frame_height - header_h`, so the oversize test subtracts it; regression test `test_pdf_callout_that_fits_a_page_is_not_split`
  - log: 2026-07-23T00:00:00Z @kj hardened: a UX round pointed out the fix was PDF-only - `splitInRow` means nothing to Word, which breaks a row wherever the page ends unless the row carries `w:cantSplit`, so the reported symptom survived in the .docx. Every content-table row now carries it; a row taller than a page still breaks, because `w:cantSplit` is a request Word drops when it cannot be met and nothing here sets an exact `w:trHeight` (the setting that would make Word clip instead). Regression test `test_docx_rows_are_marked_unbreakable`
  - log: 2026-08-27T00:00:00Z @kj edited severity

## Export response `EXPO`

- [x] `DEF-EXPO-21` **a non-ASCII filename fails the export outright** - CRITICAL; exporting `zniesławienie-milena-kabza-2026.md` returned HTTP 500 `('Unsafe header value %r', ...)` from every format; cause: the three handlers interpolated the name straight into `Content-Disposition` (`f'attachment; filename="{file_path.stem}.docx"'`) and Tornado encodes header values as latin-1, which has no mapping for `ł` (U+0142), so `set_header` raised before a byte of the document was written; fix: one `set_attachment_filename` helper on `ExportHandlerBase` emitting RFC 6266 - an ASCII `filename` for pre-standard clients plus `filename*=UTF-8''` carrying the real name percent-encoded; `routes.py`
  - log: 2026-08-17T00:00:00Z @kj reported from the browser and reproduced through the REST API: identical 500 on both paths, so frontend-independent. Not DOCX-specific either - PDF and HTML carried the same interpolation and would fail on the same name
  - log: 2026-08-17T00:00:00Z @kj fixed: all three call sites now go through the helper. The ASCII fallback maps stroked letters (`ł`, `đ`, `ø`) before NFKD, which decomposes the accented Latin letters but leaves stroked ones alone - without the map `zniesławienie` degraded to `zniesawienie`. A name in a non-Latin script leaves no ASCII at all, so the stem falls back to `export` rather than emitting the nameless dotfile `.docx`, and anything outside `[A-Za-z0-9._-]` is replaced so a quote cannot close the header value early
  - log: 2026-08-17T00:00:00Z @kj tests: four regressions in `test_routes.py` - the reported filename, ASCII unchanged, non-Latin script keeps a name, and header injection via `"` and `;`. Each asserts `value.encode('latin-1')`, the exact operation that used to raise
  - log: 2026-08-27T00:00:00Z @kj edited severity

- [x] `DEF-EXPO-54` **a sub-list behind a continuation paragraph was hoisted into the numbering above it** - MAJOR; `normalize_list_indentation` cleared its "a list is open" flag on any chunk carrying no list marker, and a loose item's own second paragraph carries none; the item's child list was then judged standalone and dedented to column 0, where python-markdown merged it into the ordered list above - `1. Step one` / a paragraph / `   - detail a` exported with the bullets renumbered as steps 2 and 3, in all three formats; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj found by the fifth round's architect lens, measured on three real documents including a README whose own cross-reference ("from step #2") then pointed at the wrong step. The flag is now cleared only by a chunk starting at column 0: an indented chunk with no marker is an item's second paragraph, not the end of the list. Regression test `test_a_sub_list_behind_a_paragraph_is_left_alone`, mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-EXPO-55` **a `# comment` inside a fenced sample was read as a heading** - MAJOR; the chunker split on any line matching an ATX heading and ended the open list there, without consulting the verbatim map the pass builds for exactly this reason; `1. Install` / a ```bash sample containing `# install`/ the item's own notes exported with the notes hoisted into the step numbering. A comment line inside a sample is the commonest line in a procedure document;`routes.py`
  - log: 2026-08-27T00:00:00Z @kj found by the fifth round's architect lens. The split now skips a line the fence scan marked verbatim. Regression test `test_a_comment_in_a_sample_is_not_a_heading`, mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-EXPO-56` **a loose indented list moved only its first item** - MAJOR; a blank line between items does not end a list, but the pass judged each chunk alone, so an indented `1.` / `2.` / `3.` list had only its first item dedented and the rest were left one level in, where a browser renumbered them 1, 1, 2 - and DOCX and PDF flattened them back to 1, 2, 3, so the three formats disagreed on the numbers the author wrote; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj found by the fifth round's architect lens, reproduced on a bundled BSD licence file. A chunk whose outermost marker sits at the column an already-dedented list was written at now takes the same shift - matched on that column, the one thing about the continuation that is not a guess. Regression test `test_a_loose_indented_list_moves_whole`, mutation-proved
  - log: 2026-08-27T00:00:00Z @kj edited severity
- [x] `DEF-EXPO-57` **an item was moved out from under the sample it owns** - MAJOR; a fenced sample's bytes stand, so it cannot travel with its item; dedenting a marker whose own sample sits at or beyond its content column left the sample indented past the item, where it rendered as an indented code block showing its own fence markers; `routes.py`
  - log: 2026-08-27T00:00:00Z @kj found by the lead's corpus differential - the single file of 8,171 that rendered worse. The pass now looks at the chunk that follows a candidate and declines to move an item that owns a sample. Regression test `test_an_item_owning_a_sample_is_left_where_it_was`, mutation-proved. With it the differential is 37 changed, 27 better, 10 identical, 0 worse
  - log: 2026-08-27T00:00:00Z @kj edited severity
